#![allow(dead_code, unused_imports)]
/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */


//! FFI bindings for `arith` (generated by tvm-ffi-stubgen).

// tvm-ffi-stubgen(begin): import-section
use std::ops::Deref;
use std::ops::DerefMut;
use super::ir::Expr;
use super::ir::ExprObj;
use super::ir::IntSet;
use super::ir::IntSetObj;
use super::ir::Range;
use super::tirx::Var;
use tvm_ffi::Array;
use tvm_ffi::Map;
use tvm_ffi::Object;
use tvm_ffi::ObjectArc;
use tvm_ffi::ObjectCore;
use tvm_ffi::ObjectRefCore;
use tvm_ffi::Result;
use tvm_ffi::String;
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.Analyzer
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.Analyzer"]
pub struct AnalyzerObj {
    base: Object,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Analyzer {
    data: ObjectArc<AnalyzerObj>,
}

impl Deref for Analyzer {
    type Target = AnalyzerObj;
    fn deref(&self) -> &AnalyzerObj {
        &self.data
    }
}

impl DerefMut for Analyzer {
    fn deref_mut(&mut self) -> &mut AnalyzerObj {
        &mut self.data
    }
}

impl Analyzer {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> AnalyzerBuilder {
        AnalyzerBuilder {
            base: Object::new(),
        }
    }
}

pub struct AnalyzerBuilder {
    base: Object,
}

impl AnalyzerBuilder {
    pub fn build(self) -> Result<Analyzer> {
        Ok(Analyzer {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<AnalyzerObj> {
        Ok(AnalyzerObj {
            base: self.base,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.CanonicalExpr
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.CanonicalExpr"]
pub struct CanonicalExprObj {
    base: ExprObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct CanonicalExpr {
    data: ObjectArc<CanonicalExprObj>,
}

impl Deref for CanonicalExpr {
    type Target = CanonicalExprObj;
    fn deref(&self) -> &CanonicalExprObj {
        &self.data
    }
}

impl Deref for CanonicalExprObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<CanonicalExpr> for Expr {
    fn from(x: CanonicalExpr) -> Expr {
        let arc = <CanonicalExpr as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl CanonicalExpr {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.ConstIntBound
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.ConstIntBound"]
pub struct ConstIntBoundObj {
    base: Object,
    pub min_value: i64,
    pub max_value: i64,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ConstIntBound {
    data: ObjectArc<ConstIntBoundObj>,
}

impl Deref for ConstIntBound {
    type Target = ConstIntBoundObj;
    fn deref(&self) -> &ConstIntBoundObj {
        &self.data
    }
}

impl ConstIntBound {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> ConstIntBoundBuilder {
        ConstIntBoundBuilder {
            base: Object::new(),
            min_value: None,
            max_value: None,
        }
    }
}

pub struct ConstIntBoundBuilder {
    base: Object,
    min_value: Option<i64>,
    max_value: Option<i64>,
}

impl ConstIntBoundBuilder {
    pub fn min_value(mut self, min_value: i64) -> Self {
        self.min_value = Some(min_value);
        self
    }

    pub fn max_value(mut self, max_value: i64) -> Self {
        self.max_value = Some(max_value);
        self
    }

    pub fn build(self) -> Result<ConstIntBound> {
        Ok(ConstIntBound {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<ConstIntBoundObj> {
        let min_value = self.min_value.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `min_value` is not set", ""))?;
        let max_value = self.max_value.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `max_value` is not set", ""))?;
        Ok(ConstIntBoundObj {
            base: self.base,
            min_value,
            max_value,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IntConstraints
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IntConstraints"]
pub struct IntConstraintsObj {
    base: Object,
    pub variables: Array<Var>,
    pub ranges: Map<Var, Range>,
    pub relations: Array<Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IntConstraints {
    data: ObjectArc<IntConstraintsObj>,
}

impl Deref for IntConstraints {
    type Target = IntConstraintsObj;
    fn deref(&self) -> &IntConstraintsObj {
        &self.data
    }
}

impl IntConstraints {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> IntConstraintsBuilder {
        IntConstraintsBuilder {
            base: Object::new(),
            variables: None,
            ranges: None,
            relations: None,
        }
    }
}

pub struct IntConstraintsBuilder {
    base: Object,
    variables: Option<Array<Var>>,
    ranges: Option<Map<Var, Range>>,
    relations: Option<Array<Expr>>,
}

impl IntConstraintsBuilder {
    pub fn variables(mut self, variables: Array<Var>) -> Self {
        self.variables = Some(variables);
        self
    }

    pub fn ranges(mut self, ranges: Map<Var, Range>) -> Self {
        self.ranges = Some(ranges);
        self
    }

    pub fn relations(mut self, relations: Array<Expr>) -> Self {
        self.relations = Some(relations);
        self
    }

    pub fn build(self) -> Result<IntConstraints> {
        Ok(IntConstraints {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<IntConstraintsObj> {
        let variables = self.variables.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `variables` is not set", ""))?;
        let ranges = self.ranges.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `ranges` is not set", ""))?;
        let relations = self.relations.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `relations` is not set", ""))?;
        Ok(IntConstraintsObj {
            base: self.base,
            variables,
            ranges,
            relations,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IntConstraintsTransform
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IntConstraintsTransform"]
pub struct IntConstraintsTransformObj {
    base: Object,
    pub src: IntConstraints,
    pub dst: IntConstraints,
    pub src_to_dst: Map<Var, Expr>,
    pub dst_to_src: Map<Var, Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IntConstraintsTransform {
    data: ObjectArc<IntConstraintsTransformObj>,
}

impl Deref for IntConstraintsTransform {
    type Target = IntConstraintsTransformObj;
    fn deref(&self) -> &IntConstraintsTransformObj {
        &self.data
    }
}

impl IntConstraintsTransform {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> IntConstraintsTransformBuilder {
        IntConstraintsTransformBuilder {
            base: Object::new(),
            src: None,
            dst: None,
            src_to_dst: None,
            dst_to_src: None,
        }
    }
}

pub struct IntConstraintsTransformBuilder {
    base: Object,
    src: Option<IntConstraints>,
    dst: Option<IntConstraints>,
    src_to_dst: Option<Map<Var, Expr>>,
    dst_to_src: Option<Map<Var, Expr>>,
}

impl IntConstraintsTransformBuilder {
    pub fn src(mut self, src: IntConstraints) -> Self {
        self.src = Some(src);
        self
    }

    pub fn dst(mut self, dst: IntConstraints) -> Self {
        self.dst = Some(dst);
        self
    }

    pub fn src_to_dst(mut self, src_to_dst: Map<Var, Expr>) -> Self {
        self.src_to_dst = Some(src_to_dst);
        self
    }

    pub fn dst_to_src(mut self, dst_to_src: Map<Var, Expr>) -> Self {
        self.dst_to_src = Some(dst_to_src);
        self
    }

    pub fn build(self) -> Result<IntConstraintsTransform> {
        Ok(IntConstraintsTransform {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<IntConstraintsTransformObj> {
        let src = self.src.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `src` is not set", ""))?;
        let dst = self.dst.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `dst` is not set", ""))?;
        let src_to_dst = self.src_to_dst.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `src_to_dst` is not set", ""))?;
        let dst_to_src = self.dst_to_src.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `dst_to_src` is not set", ""))?;
        Ok(IntConstraintsTransformObj {
            base: self.base,
            src,
            dst,
            src_to_dst,
            dst_to_src,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IntGroupBounds
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IntGroupBounds"]
pub struct IntGroupBoundsObj {
    base: Object,
    pub coef: Expr,
    pub lower: Array<Expr>,
    pub equal: Array<Expr>,
    pub upper: Array<Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IntGroupBounds {
    data: ObjectArc<IntGroupBoundsObj>,
}

impl Deref for IntGroupBounds {
    type Target = IntGroupBoundsObj;
    fn deref(&self) -> &IntGroupBoundsObj {
        &self.data
    }
}

impl IntGroupBounds {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> IntGroupBoundsBuilder {
        IntGroupBoundsBuilder {
            base: Object::new(),
            coef: None,
            lower: None,
            equal: None,
            upper: None,
        }
    }
}

pub struct IntGroupBoundsBuilder {
    base: Object,
    coef: Option<Expr>,
    lower: Option<Array<Expr>>,
    equal: Option<Array<Expr>>,
    upper: Option<Array<Expr>>,
}

impl IntGroupBoundsBuilder {
    pub fn coef(mut self, coef: Expr) -> Self {
        self.coef = Some(coef);
        self
    }

    pub fn lower(mut self, lower: Array<Expr>) -> Self {
        self.lower = Some(lower);
        self
    }

    pub fn equal(mut self, equal: Array<Expr>) -> Self {
        self.equal = Some(equal);
        self
    }

    pub fn upper(mut self, upper: Array<Expr>) -> Self {
        self.upper = Some(upper);
        self
    }

    pub fn build(self) -> Result<IntGroupBounds> {
        Ok(IntGroupBounds {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<IntGroupBoundsObj> {
        let coef = self.coef.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `coef` is not set", ""))?;
        let lower = self.lower.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `lower` is not set", ""))?;
        let equal = self.equal.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `equal` is not set", ""))?;
        let upper = self.upper.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `upper` is not set", ""))?;
        Ok(IntGroupBoundsObj {
            base: self.base,
            coef,
            lower,
            equal,
            upper,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IntervalSet
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IntervalSet"]
pub struct IntervalSetObj {
    base: IntSetObj,
    pub min_value: Expr,
    pub max_value: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IntervalSet {
    data: ObjectArc<IntervalSetObj>,
}

impl Deref for IntervalSet {
    type Target = IntervalSetObj;
    fn deref(&self) -> &IntervalSetObj {
        &self.data
    }
}

impl Deref for IntervalSetObj {
    type Target = IntSetObj;
    fn deref(&self) -> &IntSetObj {
        &self.base
    }
}

impl From<IntervalSet> for IntSet {
    fn from(x: IntervalSet) -> IntSet {
        let arc = <IntervalSet as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const IntSetObj)
        };
        <IntSet as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl IntervalSet {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IterMapExpr
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IterMapExpr"]
pub struct IterMapExprObj {
    base: ExprObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IterMapExpr {
    data: ObjectArc<IterMapExprObj>,
}

impl Deref for IterMapExpr {
    type Target = IterMapExprObj;
    fn deref(&self) -> &IterMapExprObj {
        &self.data
    }
}

impl Deref for IterMapExprObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<IterMapExpr> for Expr {
    fn from(x: IterMapExpr) -> Expr {
        let arc = <IterMapExpr as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl IterMapExpr {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IterMapResult
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IterMapResult"]
pub struct IterMapResultObj {
    base: Object,
    pub indices: Array<IterSumExpr>,
    pub errors: Array<String>,
    pub padding_predicate: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IterMapResult {
    data: ObjectArc<IterMapResultObj>,
}

impl Deref for IterMapResult {
    type Target = IterMapResultObj;
    fn deref(&self) -> &IterMapResultObj {
        &self.data
    }
}

impl IterMapResult {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> IterMapResultBuilder {
        IterMapResultBuilder {
            base: Object::new(),
            indices: None,
            errors: None,
            padding_predicate: None,
        }
    }
}

pub struct IterMapResultBuilder {
    base: Object,
    indices: Option<Array<IterSumExpr>>,
    errors: Option<Array<String>>,
    padding_predicate: Option<Expr>,
}

impl IterMapResultBuilder {
    pub fn indices(mut self, indices: Array<IterSumExpr>) -> Self {
        self.indices = Some(indices);
        self
    }

    pub fn errors(mut self, errors: Array<String>) -> Self {
        self.errors = Some(errors);
        self
    }

    pub fn padding_predicate(mut self, padding_predicate: Expr) -> Self {
        self.padding_predicate = Some(padding_predicate);
        self
    }

    pub fn build(self) -> Result<IterMapResult> {
        Ok(IterMapResult {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<IterMapResultObj> {
        let indices = self.indices.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `indices` is not set", ""))?;
        let errors = self.errors.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `errors` is not set", ""))?;
        let padding_predicate = self.padding_predicate.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `padding_predicate` is not set", ""))?;
        Ok(IterMapResultObj {
            base: self.base,
            indices,
            errors,
            padding_predicate,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IterMark
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IterMark"]
pub struct IterMarkObj {
    base: Object,
    pub source: Expr,
    pub extent: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IterMark {
    data: ObjectArc<IterMarkObj>,
}

impl Deref for IterMark {
    type Target = IterMarkObj;
    fn deref(&self) -> &IterMarkObj {
        &self.data
    }
}

impl IterMark {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> IterMarkBuilder {
        IterMarkBuilder {
            base: Object::new(),
            source: None,
            extent: None,
        }
    }
}

pub struct IterMarkBuilder {
    base: Object,
    source: Option<Expr>,
    extent: Option<Expr>,
}

impl IterMarkBuilder {
    pub fn source(mut self, source: Expr) -> Self {
        self.source = Some(source);
        self
    }

    pub fn extent(mut self, extent: Expr) -> Self {
        self.extent = Some(extent);
        self
    }

    pub fn build(self) -> Result<IterMark> {
        Ok(IterMark {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<IterMarkObj> {
        let source = self.source.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `source` is not set", ""))?;
        let extent = self.extent.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `extent` is not set", ""))?;
        Ok(IterMarkObj {
            base: self.base,
            source,
            extent,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IterSplitExpr
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IterSplitExpr"]
pub struct IterSplitExprObj {
    base: IterMapExprObj,
    pub source: IterMark,
    pub lower_factor: Expr,
    pub extent: Expr,
    pub scale: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IterSplitExpr {
    data: ObjectArc<IterSplitExprObj>,
}

impl Deref for IterSplitExpr {
    type Target = IterSplitExprObj;
    fn deref(&self) -> &IterSplitExprObj {
        &self.data
    }
}

impl Deref for IterSplitExprObj {
    type Target = IterMapExprObj;
    fn deref(&self) -> &IterMapExprObj {
        &self.base
    }
}

impl From<IterSplitExpr> for IterMapExpr {
    fn from(x: IterSplitExpr) -> IterMapExpr {
        let arc = <IterSplitExpr as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const IterMapExprObj)
        };
        <IterMapExpr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl IterSplitExpr {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.IterSumExpr
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.IterSumExpr"]
pub struct IterSumExprObj {
    __base: IterMapExprObj,
    pub args: Array<IterSplitExpr>,
    pub base: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IterSumExpr {
    data: ObjectArc<IterSumExprObj>,
}

impl Deref for IterSumExpr {
    type Target = IterSumExprObj;
    fn deref(&self) -> &IterSumExprObj {
        &self.data
    }
}

impl Deref for IterSumExprObj {
    type Target = IterMapExprObj;
    fn deref(&self) -> &IterMapExprObj {
        &self.__base
    }
}

impl From<IterSumExpr> for IterMapExpr {
    fn from(x: IterSumExpr) -> IterMapExpr {
        let arc = <IterSumExpr as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const IterMapExprObj)
        };
        <IterMapExpr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl IterSumExpr {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.ModularSet
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.ModularSet"]
pub struct ModularSetObj {
    __base: Object,
    pub coeff: i64,
    pub base: i64,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ModularSet {
    data: ObjectArc<ModularSetObj>,
}

impl Deref for ModularSet {
    type Target = ModularSetObj;
    fn deref(&self) -> &ModularSetObj {
        &self.data
    }
}

impl ModularSet {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> ModularSetBuilder {
        ModularSetBuilder {
            __base: Object::new(),
            coeff: None,
            base: None,
        }
    }
}

pub struct ModularSetBuilder {
    __base: Object,
    coeff: Option<i64>,
    base: Option<i64>,
}

impl ModularSetBuilder {
    pub fn coeff(mut self, coeff: i64) -> Self {
        self.coeff = Some(coeff);
        self
    }

    pub fn base(mut self, base: i64) -> Self {
        self.base = Some(base);
        self
    }

    pub fn build(self) -> Result<ModularSet> {
        Ok(ModularSet {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<ModularSetObj> {
        let coeff = self.coeff.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `coeff` is not set", ""))?;
        let base = self.base.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `base` is not set", ""))?;
        Ok(ModularSetObj {
            __base: self.__base,
            coeff,
            base,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.PresburgerSet
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.PresburgerSet"]
pub struct PresburgerSetObj {
    base: IntSetObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PresburgerSet {
    data: ObjectArc<PresburgerSetObj>,
}

impl Deref for PresburgerSet {
    type Target = PresburgerSetObj;
    fn deref(&self) -> &PresburgerSetObj {
        &self.data
    }
}

impl Deref for PresburgerSetObj {
    type Target = IntSetObj;
    fn deref(&self) -> &IntSetObj {
        &self.base
    }
}

impl From<PresburgerSet> for IntSet {
    fn from(x: PresburgerSet) -> IntSet {
        let arc = <PresburgerSet as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const IntSetObj)
        };
        <IntSet as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl PresburgerSet {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.RewriteSimplifierStats
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.RewriteSimplifierStats"]
pub struct RewriteSimplifierStatsObj {
    base: Object,
    pub nodes_visited: i64,
    pub constraints_entered: i64,
    pub rewrites_attempted: i64,
    pub rewrites_performed: i64,
    pub max_recursive_depth: i64,
    pub num_recursive_rewrites: i64,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct RewriteSimplifierStats {
    data: ObjectArc<RewriteSimplifierStatsObj>,
}

impl Deref for RewriteSimplifierStats {
    type Target = RewriteSimplifierStatsObj;
    fn deref(&self) -> &RewriteSimplifierStatsObj {
        &self.data
    }
}

impl RewriteSimplifierStats {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> RewriteSimplifierStatsBuilder {
        RewriteSimplifierStatsBuilder {
            base: Object::new(),
            nodes_visited: None,
            constraints_entered: None,
            rewrites_attempted: None,
            rewrites_performed: None,
            max_recursive_depth: None,
            num_recursive_rewrites: None,
        }
    }
}

pub struct RewriteSimplifierStatsBuilder {
    base: Object,
    nodes_visited: Option<i64>,
    constraints_entered: Option<i64>,
    rewrites_attempted: Option<i64>,
    rewrites_performed: Option<i64>,
    max_recursive_depth: Option<i64>,
    num_recursive_rewrites: Option<i64>,
}

impl RewriteSimplifierStatsBuilder {
    pub fn nodes_visited(mut self, nodes_visited: i64) -> Self {
        self.nodes_visited = Some(nodes_visited);
        self
    }

    pub fn constraints_entered(mut self, constraints_entered: i64) -> Self {
        self.constraints_entered = Some(constraints_entered);
        self
    }

    pub fn rewrites_attempted(mut self, rewrites_attempted: i64) -> Self {
        self.rewrites_attempted = Some(rewrites_attempted);
        self
    }

    pub fn rewrites_performed(mut self, rewrites_performed: i64) -> Self {
        self.rewrites_performed = Some(rewrites_performed);
        self
    }

    pub fn max_recursive_depth(mut self, max_recursive_depth: i64) -> Self {
        self.max_recursive_depth = Some(max_recursive_depth);
        self
    }

    pub fn num_recursive_rewrites(mut self, num_recursive_rewrites: i64) -> Self {
        self.num_recursive_rewrites = Some(num_recursive_rewrites);
        self
    }

    pub fn build(self) -> Result<RewriteSimplifierStats> {
        Ok(RewriteSimplifierStats {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<RewriteSimplifierStatsObj> {
        let nodes_visited = self.nodes_visited.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `nodes_visited` is not set", ""))?;
        let constraints_entered = self.constraints_entered.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `constraints_entered` is not set", ""))?;
        let rewrites_attempted = self.rewrites_attempted.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `rewrites_attempted` is not set", ""))?;
        let rewrites_performed = self.rewrites_performed.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `rewrites_performed` is not set", ""))?;
        let max_recursive_depth = self.max_recursive_depth.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `max_recursive_depth` is not set", ""))?;
        let num_recursive_rewrites = self.num_recursive_rewrites.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `num_recursive_rewrites` is not set", ""))?;
        Ok(RewriteSimplifierStatsObj {
            base: self.base,
            nodes_visited,
            constraints_entered,
            rewrites_attempted,
            rewrites_performed,
            max_recursive_depth,
            num_recursive_rewrites,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.SplitExpr
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.SplitExpr"]
pub struct SplitExprObj {
    base: CanonicalExprObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct SplitExpr {
    data: ObjectArc<SplitExprObj>,
}

impl Deref for SplitExpr {
    type Target = SplitExprObj;
    fn deref(&self) -> &SplitExprObj {
        &self.data
    }
}

impl Deref for SplitExprObj {
    type Target = CanonicalExprObj;
    fn deref(&self) -> &CanonicalExprObj {
        &self.base
    }
}

impl From<SplitExpr> for CanonicalExpr {
    fn from(x: SplitExpr) -> CanonicalExpr {
        let arc = <SplitExpr as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const CanonicalExprObj)
        };
        <CanonicalExpr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl SplitExpr {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/arith.SumExpr
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "arith.SumExpr"]
pub struct SumExprObj {
    base: CanonicalExprObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct SumExpr {
    data: ObjectArc<SumExprObj>,
}

impl Deref for SumExpr {
    type Target = SumExprObj;
    fn deref(&self) -> &SumExprObj {
        &self.data
    }
}

impl Deref for SumExprObj {
    type Target = CanonicalExprObj;
    fn deref(&self) -> &CanonicalExprObj {
        &self.base
    }
}

impl From<SumExpr> for CanonicalExpr {
    fn from(x: SumExpr) -> CanonicalExpr {
        let arc = <SumExpr as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const CanonicalExprObj)
        };
        <CanonicalExpr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl SumExpr {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)
