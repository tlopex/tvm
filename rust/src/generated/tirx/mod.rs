#![allow(dead_code, unused_imports)]
/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */


//! FFI bindings for `tirx` (generated by tvm-ffi-stubgen).

// tvm-ffi-stubgen(begin): import-section
use std::ops::Deref;
use std::ops::DerefMut;
use super::ir::BaseFunc;
use super::ir::BaseFuncObj;
use super::ir::Expr;
use super::ir::ExprObj;
use super::ir::IntImm;
use super::ir::Op;
use super::ir::PrimExprConvertible;
use super::ir::PrimExprConvertibleObj;
use super::ir::PrimType;
use super::ir::Range;
use super::ir::Span;
use super::ir::Type;
use super::target::Target;
use super::transform::Pass;
use super::transform::PassInfo;
use super::transform::PassObj;
use tvm_ffi::Any;
use tvm_ffi::Array;
use tvm_ffi::Map;
use tvm_ffi::Object;
use tvm_ffi::ObjectArc;
use tvm_ffi::ObjectCore;
use tvm_ffi::ObjectRefCore;
use tvm_ffi::Optional;
use tvm_ffi::Result;
use tvm_ffi::String;
use tvm_ffi::object::ObjectRef;
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Add
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Add"]
pub struct AddObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Add {
    data: ObjectArc<AddObj>,
}

impl Deref for Add {
    type Target = AddObj;
    fn deref(&self) -> &AddObj {
        &self.data
    }
}

impl Deref for AddObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Add> for Expr {
    fn from(x: Add) -> Expr {
        let arc = <Add as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Add {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.And
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.And"]
pub struct AndObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct And {
    data: ObjectArc<AndObj>,
}

impl Deref for And {
    type Target = AndObj;
    fn deref(&self) -> &AndObj {
        &self.data
    }
}

impl Deref for AndObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<And> for Expr {
    fn from(x: And) -> Expr {
        let arc = <And as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl And {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Axis
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Axis"]
pub struct AxisObj {
    base: Object,
    pub name: String,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Axis {
    data: ObjectArc<AxisObj>,
}

impl Deref for Axis {
    type Target = AxisObj;
    fn deref(&self) -> &AxisObj {
        &self.data
    }
}

impl Axis {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> AxisBuilder {
        AxisBuilder {
            base: Object::new(),
            name: None,
        }
    }
}

pub struct AxisBuilder {
    base: Object,
    name: Option<String>,
}

impl AxisBuilder {
    pub fn name(mut self, name: String) -> Self {
        self.name = Some(name);
        self
    }

    pub fn build(self) -> Result<Axis> {
        Ok(Axis {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<AxisObj> {
        let name = self.name.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `name` is not set", ""))?;
        Ok(AxisObj {
            base: self.base,
            name,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Broadcast
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Broadcast"]
pub struct BroadcastObj {
    base: ExprObj,
    pub value: Expr,
    pub lanes: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Broadcast {
    data: ObjectArc<BroadcastObj>,
}

impl Deref for Broadcast {
    type Target = BroadcastObj;
    fn deref(&self) -> &BroadcastObj {
        &self.data
    }
}

impl Deref for BroadcastObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Broadcast> for Expr {
    fn from(x: Broadcast) -> Expr {
        let arc = <Broadcast as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Broadcast {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Buffer
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Buffer"]
pub struct BufferObj {
    base: Object,
    pub data: Var,
    pub dtype: PrimType,
    pub shape: Array<Expr>,
    pub axis_separators: Array<IntImm>,
    pub strides: Array<Expr>,
    pub elem_offset: Expr,
    pub name: String,
    pub data_alignment: i32,
    pub offset_factor: i32,
    pub buffer_type: i32,
    pub span: Span,
    pub layout: Optional<Layout>,
    pub allocated_addr: Array<Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Buffer {
    data: ObjectArc<BufferObj>,
}

impl Deref for Buffer {
    type Target = BufferObj;
    fn deref(&self) -> &BufferObj {
        &self.data
    }
}

impl Buffer {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> BufferBuilder {
        BufferBuilder {
            base: Object::new(),
            data: None,
            dtype: None,
            shape: None,
            axis_separators: None,
            strides: None,
            elem_offset: None,
            name: None,
            data_alignment: None,
            offset_factor: None,
            buffer_type: None,
            span: None,
            layout: None,
            allocated_addr: None,
        }
    }
}

pub struct BufferBuilder {
    base: Object,
    data: Option<Var>,
    dtype: Option<PrimType>,
    shape: Option<Array<Expr>>,
    axis_separators: Option<Array<IntImm>>,
    strides: Option<Array<Expr>>,
    elem_offset: Option<Expr>,
    name: Option<String>,
    data_alignment: Option<i32>,
    offset_factor: Option<i32>,
    buffer_type: Option<i32>,
    span: Option<Span>,
    layout: Option<Optional<Layout>>,
    allocated_addr: Option<Array<Expr>>,
}

impl BufferBuilder {
    pub fn data(mut self, data: Var) -> Self {
        self.data = Some(data);
        self
    }

    pub fn dtype(mut self, dtype: PrimType) -> Self {
        self.dtype = Some(dtype);
        self
    }

    pub fn shape(mut self, shape: Array<Expr>) -> Self {
        self.shape = Some(shape);
        self
    }

    pub fn axis_separators(mut self, axis_separators: Array<IntImm>) -> Self {
        self.axis_separators = Some(axis_separators);
        self
    }

    pub fn strides(mut self, strides: Array<Expr>) -> Self {
        self.strides = Some(strides);
        self
    }

    pub fn elem_offset(mut self, elem_offset: Expr) -> Self {
        self.elem_offset = Some(elem_offset);
        self
    }

    pub fn name(mut self, name: String) -> Self {
        self.name = Some(name);
        self
    }

    pub fn data_alignment(mut self, data_alignment: i32) -> Self {
        self.data_alignment = Some(data_alignment);
        self
    }

    pub fn offset_factor(mut self, offset_factor: i32) -> Self {
        self.offset_factor = Some(offset_factor);
        self
    }

    pub fn buffer_type(mut self, buffer_type: i32) -> Self {
        self.buffer_type = Some(buffer_type);
        self
    }

    pub fn span(mut self, span: Span) -> Self {
        self.span = Some(span);
        self
    }

    pub fn layout(mut self, layout: Optional<Layout>) -> Self {
        self.layout = Some(layout);
        self
    }

    pub fn allocated_addr(mut self, allocated_addr: Array<Expr>) -> Self {
        self.allocated_addr = Some(allocated_addr);
        self
    }

    pub fn build(self) -> Result<Buffer> {
        Ok(Buffer {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<BufferObj> {
        let data = self.data.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `data` is not set", ""))?;
        let dtype = self.dtype.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `dtype` is not set", ""))?;
        let shape = self.shape.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `shape` is not set", ""))?;
        let axis_separators = self.axis_separators.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `axis_separators` is not set", ""))?;
        let strides = self.strides.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `strides` is not set", ""))?;
        let elem_offset = self.elem_offset.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `elem_offset` is not set", ""))?;
        let name = self.name.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `name` is not set", ""))?;
        let data_alignment = self.data_alignment.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `data_alignment` is not set", ""))?;
        let offset_factor = self.offset_factor.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `offset_factor` is not set", ""))?;
        let buffer_type = self.buffer_type.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `buffer_type` is not set", ""))?;
        let span = self.span.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `span` is not set", ""))?;
        let layout = self.layout.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `layout` is not set", ""))?;
        let allocated_addr = self.allocated_addr.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `allocated_addr` is not set", ""))?;
        Ok(BufferObj {
            base: self.base,
            data,
            dtype,
            shape,
            axis_separators,
            strides,
            elem_offset,
            name,
            data_alignment,
            offset_factor,
            buffer_type,
            span,
            layout,
            allocated_addr,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.BufferLoad
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.BufferLoad"]
pub struct BufferLoadObj {
    base: ExprObj,
    pub buffer: Buffer,
    pub indices: Array<Expr>,
    pub predicate: Optional<Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct BufferLoad {
    data: ObjectArc<BufferLoadObj>,
}

impl Deref for BufferLoad {
    type Target = BufferLoadObj;
    fn deref(&self) -> &BufferLoadObj {
        &self.data
    }
}

impl Deref for BufferLoadObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<BufferLoad> for Expr {
    fn from(x: BufferLoad) -> Expr {
        let arc = <BufferLoad as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl BufferLoad {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.BufferRegion
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.BufferRegion"]
pub struct BufferRegionObj {
    base: PrimExprConvertibleObj,
    pub buffer: Buffer,
    pub region: Array<Range>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct BufferRegion {
    data: ObjectArc<BufferRegionObj>,
}

impl Deref for BufferRegion {
    type Target = BufferRegionObj;
    fn deref(&self) -> &BufferRegionObj {
        &self.data
    }
}

impl Deref for BufferRegionObj {
    type Target = PrimExprConvertibleObj;
    fn deref(&self) -> &PrimExprConvertibleObj {
        &self.base
    }
}

impl From<BufferRegion> for PrimExprConvertible {
    fn from(x: BufferRegion) -> PrimExprConvertible {
        let arc = <BufferRegion as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PrimExprConvertibleObj)
        };
        <PrimExprConvertible as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl BufferRegion {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Cast
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Cast"]
pub struct CastObj {
    base: ExprObj,
    pub value: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Cast {
    data: ObjectArc<CastObj>,
}

impl Deref for Cast {
    type Target = CastObj;
    fn deref(&self) -> &CastObj {
        &self.data
    }
}

impl Deref for CastObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Cast> for Expr {
    fn from(x: Cast) -> Expr {
        let arc = <Cast as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Cast {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.CommReducer
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.CommReducer"]
pub struct CommReducerObj {
    base: Object,
    pub lhs: Array<Var>,
    pub rhs: Array<Var>,
    pub result: Array<Expr>,
    pub identity_element: Array<Expr>,
    pub span: Span,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct CommReducer {
    data: ObjectArc<CommReducerObj>,
}

impl Deref for CommReducer {
    type Target = CommReducerObj;
    fn deref(&self) -> &CommReducerObj {
        &self.data
    }
}

impl CommReducer {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> CommReducerBuilder {
        CommReducerBuilder {
            base: Object::new(),
            lhs: None,
            rhs: None,
            result: None,
            identity_element: None,
            span: None,
        }
    }
}

pub struct CommReducerBuilder {
    base: Object,
    lhs: Option<Array<Var>>,
    rhs: Option<Array<Var>>,
    result: Option<Array<Expr>>,
    identity_element: Option<Array<Expr>>,
    span: Option<Span>,
}

impl CommReducerBuilder {
    pub fn lhs(mut self, lhs: Array<Var>) -> Self {
        self.lhs = Some(lhs);
        self
    }

    pub fn rhs(mut self, rhs: Array<Var>) -> Self {
        self.rhs = Some(rhs);
        self
    }

    pub fn result(mut self, result: Array<Expr>) -> Self {
        self.result = Some(result);
        self
    }

    pub fn identity_element(mut self, identity_element: Array<Expr>) -> Self {
        self.identity_element = Some(identity_element);
        self
    }

    pub fn span(mut self, span: Span) -> Self {
        self.span = Some(span);
        self
    }

    pub fn build(self) -> Result<CommReducer> {
        Ok(CommReducer {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<CommReducerObj> {
        let lhs = self.lhs.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `lhs` is not set", ""))?;
        let rhs = self.rhs.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `rhs` is not set", ""))?;
        let result = self.result.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `result` is not set", ""))?;
        let identity_element = self.identity_element.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `identity_element` is not set", ""))?;
        let span = self.span.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `span` is not set", ""))?;
        Ok(CommReducerObj {
            base: self.base,
            lhs,
            rhs,
            result,
            identity_element,
            span,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.DataProducer
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.DataProducer"]
pub struct DataProducerObj {
    base: PrimExprConvertibleObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct DataProducer {
    data: ObjectArc<DataProducerObj>,
}

impl Deref for DataProducer {
    type Target = DataProducerObj;
    fn deref(&self) -> &DataProducerObj {
        &self.data
    }
}

impl Deref for DataProducerObj {
    type Target = PrimExprConvertibleObj;
    fn deref(&self) -> &PrimExprConvertibleObj {
        &self.base
    }
}

impl From<DataProducer> for PrimExprConvertible {
    fn from(x: DataProducer) -> PrimExprConvertible {
        let arc = <DataProducer as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PrimExprConvertibleObj)
        };
        <PrimExprConvertible as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl DataProducer {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.DispatchContext
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.DispatchContext"]
pub struct DispatchContextObj {
    base: Object,
    pub target: Target,
    pub exec_scope: ExecScope,
    pub launch_params: Map<String, IterVar>,
    pub var_range_map: Map<Var, Range>,
    pub alloc_only: bool,
    pub callbacks: Map<String, ObjectRef>,
    pub shared_state: Map<String, ObjectRef>,
    pub inter: Map<String, Array<Expr>>,
    pub intra: Map<String, Array<Expr>>,
    pub scope_kind: String,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct DispatchContext {
    data: ObjectArc<DispatchContextObj>,
}

impl Deref for DispatchContext {
    type Target = DispatchContextObj;
    fn deref(&self) -> &DispatchContextObj {
        &self.data
    }
}

impl DispatchContext {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> DispatchContextBuilder {
        DispatchContextBuilder {
            base: Object::new(),
            target: None,
            exec_scope: None,
            launch_params: None,
            var_range_map: None,
            alloc_only: None,
            callbacks: None,
            shared_state: None,
            inter: None,
            intra: None,
            scope_kind: None,
        }
    }
}

pub struct DispatchContextBuilder {
    base: Object,
    target: Option<Target>,
    exec_scope: Option<ExecScope>,
    launch_params: Option<Map<String, IterVar>>,
    var_range_map: Option<Map<Var, Range>>,
    alloc_only: Option<bool>,
    callbacks: Option<Map<String, ObjectRef>>,
    shared_state: Option<Map<String, ObjectRef>>,
    inter: Option<Map<String, Array<Expr>>>,
    intra: Option<Map<String, Array<Expr>>>,
    scope_kind: Option<String>,
}

impl DispatchContextBuilder {
    pub fn target(mut self, target: Target) -> Self {
        self.target = Some(target);
        self
    }

    pub fn exec_scope(mut self, exec_scope: ExecScope) -> Self {
        self.exec_scope = Some(exec_scope);
        self
    }

    pub fn launch_params(mut self, launch_params: Map<String, IterVar>) -> Self {
        self.launch_params = Some(launch_params);
        self
    }

    pub fn var_range_map(mut self, var_range_map: Map<Var, Range>) -> Self {
        self.var_range_map = Some(var_range_map);
        self
    }

    pub fn alloc_only(mut self, alloc_only: bool) -> Self {
        self.alloc_only = Some(alloc_only);
        self
    }

    pub fn callbacks(mut self, callbacks: Map<String, ObjectRef>) -> Self {
        self.callbacks = Some(callbacks);
        self
    }

    pub fn shared_state(mut self, shared_state: Map<String, ObjectRef>) -> Self {
        self.shared_state = Some(shared_state);
        self
    }

    pub fn inter(mut self, inter: Map<String, Array<Expr>>) -> Self {
        self.inter = Some(inter);
        self
    }

    pub fn intra(mut self, intra: Map<String, Array<Expr>>) -> Self {
        self.intra = Some(intra);
        self
    }

    pub fn scope_kind(mut self, scope_kind: String) -> Self {
        self.scope_kind = Some(scope_kind);
        self
    }

    pub fn build(self) -> Result<DispatchContext> {
        Ok(DispatchContext {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<DispatchContextObj> {
        let target = self.target.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `target` is not set", ""))?;
        let exec_scope = self.exec_scope.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `exec_scope` is not set", ""))?;
        let launch_params = self.launch_params.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `launch_params` is not set", ""))?;
        let var_range_map = self.var_range_map.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `var_range_map` is not set", ""))?;
        let alloc_only = self.alloc_only.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `alloc_only` is not set", ""))?;
        let callbacks = self.callbacks.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `callbacks` is not set", ""))?;
        let shared_state = self.shared_state.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `shared_state` is not set", ""))?;
        let inter = self.inter.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `inter` is not set", ""))?;
        let intra = self.intra.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `intra` is not set", ""))?;
        let scope_kind = self.scope_kind.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `scope_kind` is not set", ""))?;
        Ok(DispatchContextObj {
            base: self.base,
            target,
            exec_scope,
            launch_params,
            var_range_map,
            alloc_only,
            callbacks,
            shared_state,
            inter,
            intra,
            scope_kind,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Div
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Div"]
pub struct DivObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Div {
    data: ObjectArc<DivObj>,
}

impl Deref for Div {
    type Target = DivObj;
    fn deref(&self) -> &DivObj {
        &self.data
    }
}

impl Deref for DivObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Div> for Expr {
    fn from(x: Div) -> Expr {
        let arc = <Div as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Div {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.EQ
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.EQ"]
pub struct EQObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct EQ {
    data: ObjectArc<EQObj>,
}

impl Deref for EQ {
    type Target = EQObj;
    fn deref(&self) -> &EQObj {
        &self.data
    }
}

impl Deref for EQObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<EQ> for Expr {
    fn from(x: EQ) -> Expr {
        let arc = <EQ as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl EQ {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.ExecScope
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.ExecScope"]
pub struct ExecScopeObj {
    base: Object,
    pub kind: i32,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ExecScope {
    data: ObjectArc<ExecScopeObj>,
}

impl Deref for ExecScope {
    type Target = ExecScopeObj;
    fn deref(&self) -> &ExecScopeObj {
        &self.data
    }
}

impl ExecScope {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> ExecScopeBuilder {
        ExecScopeBuilder {
            base: Object::new(),
            kind: None,
        }
    }
}

pub struct ExecScopeBuilder {
    base: Object,
    kind: Option<i32>,
}

impl ExecScopeBuilder {
    pub fn kind(mut self, kind: i32) -> Self {
        self.kind = Some(kind);
        self
    }

    pub fn build(self) -> Result<ExecScope> {
        Ok(ExecScope {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<ExecScopeObj> {
        let kind = self.kind.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `kind` is not set", ""))?;
        Ok(ExecScopeObj {
            base: self.base,
            kind,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.FloorDiv
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.FloorDiv"]
pub struct FloorDivObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct FloorDiv {
    data: ObjectArc<FloorDivObj>,
}

impl Deref for FloorDiv {
    type Target = FloorDivObj;
    fn deref(&self) -> &FloorDivObj {
        &self.data
    }
}

impl Deref for FloorDivObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<FloorDiv> for Expr {
    fn from(x: FloorDiv) -> Expr {
        let arc = <FloorDiv as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl FloorDiv {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.FloorMod
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.FloorMod"]
pub struct FloorModObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct FloorMod {
    data: ObjectArc<FloorModObj>,
}

impl Deref for FloorMod {
    type Target = FloorModObj;
    fn deref(&self) -> &FloorModObj {
        &self.data
    }
}

impl Deref for FloorModObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<FloorMod> for Expr {
    fn from(x: FloorMod) -> Expr {
        let arc = <FloorMod as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl FloorMod {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.GE
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.GE"]
pub struct GEObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct GE {
    data: ObjectArc<GEObj>,
}

impl Deref for GE {
    type Target = GEObj;
    fn deref(&self) -> &GEObj {
        &self.data
    }
}

impl Deref for GEObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<GE> for Expr {
    fn from(x: GE) -> Expr {
        let arc = <GE as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl GE {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.GT
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.GT"]
pub struct GTObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct GT {
    data: ObjectArc<GTObj>,
}

impl Deref for GT {
    type Target = GTObj;
    fn deref(&self) -> &GTObj {
        &self.data
    }
}

impl Deref for GTObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<GT> for Expr {
    fn from(x: GT) -> Expr {
        let arc = <GT as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl GT {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.IndexMap
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.IndexMap"]
pub struct IndexMapObj {
    base: Object,
    pub initial_indices: Array<Var>,
    pub final_indices: Array<Expr>,
    pub inverse_index_map: Optional<ObjectRef>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IndexMap {
    data: ObjectArc<IndexMapObj>,
}

impl Deref for IndexMap {
    type Target = IndexMapObj;
    fn deref(&self) -> &IndexMapObj {
        &self.data
    }
}

impl IndexMap {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> IndexMapBuilder {
        IndexMapBuilder {
            base: Object::new(),
            initial_indices: None,
            final_indices: None,
            inverse_index_map: None,
        }
    }
}

pub struct IndexMapBuilder {
    base: Object,
    initial_indices: Option<Array<Var>>,
    final_indices: Option<Array<Expr>>,
    inverse_index_map: Option<Optional<ObjectRef>>,
}

impl IndexMapBuilder {
    pub fn initial_indices(mut self, initial_indices: Array<Var>) -> Self {
        self.initial_indices = Some(initial_indices);
        self
    }

    pub fn final_indices(mut self, final_indices: Array<Expr>) -> Self {
        self.final_indices = Some(final_indices);
        self
    }

    pub fn inverse_index_map(mut self, inverse_index_map: Optional<ObjectRef>) -> Self {
        self.inverse_index_map = Some(inverse_index_map);
        self
    }

    pub fn build(self) -> Result<IndexMap> {
        Ok(IndexMap {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<IndexMapObj> {
        let initial_indices = self.initial_indices.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `initial_indices` is not set", ""))?;
        let final_indices = self.final_indices.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `final_indices` is not set", ""))?;
        let inverse_index_map = self.inverse_index_map.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `inverse_index_map` is not set", ""))?;
        Ok(IndexMapObj {
            base: self.base,
            initial_indices,
            final_indices,
            inverse_index_map,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Iter
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Iter"]
pub struct IterObj {
    base: Object,
    pub extent: Expr,
    pub stride: Expr,
    pub axis: Axis,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Iter {
    data: ObjectArc<IterObj>,
}

impl Deref for Iter {
    type Target = IterObj;
    fn deref(&self) -> &IterObj {
        &self.data
    }
}

impl Iter {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> IterBuilder {
        IterBuilder {
            base: Object::new(),
            extent: None,
            stride: None,
            axis: None,
        }
    }
}

pub struct IterBuilder {
    base: Object,
    extent: Option<Expr>,
    stride: Option<Expr>,
    axis: Option<Axis>,
}

impl IterBuilder {
    pub fn extent(mut self, extent: Expr) -> Self {
        self.extent = Some(extent);
        self
    }

    pub fn stride(mut self, stride: Expr) -> Self {
        self.stride = Some(stride);
        self
    }

    pub fn axis(mut self, axis: Axis) -> Self {
        self.axis = Some(axis);
        self
    }

    pub fn build(self) -> Result<Iter> {
        Ok(Iter {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<IterObj> {
        let extent = self.extent.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `extent` is not set", ""))?;
        let stride = self.stride.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `stride` is not set", ""))?;
        let axis = self.axis.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `axis` is not set", ""))?;
        Ok(IterObj {
            base: self.base,
            extent,
            stride,
            axis,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.IterVar
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.IterVar"]
pub struct IterVarObj {
    base: PrimExprConvertibleObj,
    pub dom: Range,
    pub var: Var,
    pub iter_type: i32,
    pub thread_tag: String,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IterVar {
    data: ObjectArc<IterVarObj>,
}

impl Deref for IterVar {
    type Target = IterVarObj;
    fn deref(&self) -> &IterVarObj {
        &self.data
    }
}

impl Deref for IterVarObj {
    type Target = PrimExprConvertibleObj;
    fn deref(&self) -> &PrimExprConvertibleObj {
        &self.base
    }
}

impl From<IterVar> for PrimExprConvertible {
    fn from(x: IterVar) -> PrimExprConvertible {
        let arc = <IterVar as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PrimExprConvertibleObj)
        };
        <PrimExprConvertible as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl IterVar {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.LE
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.LE"]
pub struct LEObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct LE {
    data: ObjectArc<LEObj>,
}

impl Deref for LE {
    type Target = LEObj;
    fn deref(&self) -> &LEObj {
        &self.data
    }
}

impl Deref for LEObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<LE> for Expr {
    fn from(x: LE) -> Expr {
        let arc = <LE as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl LE {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.LT
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.LT"]
pub struct LTObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct LT {
    data: ObjectArc<LTObj>,
}

impl Deref for LT {
    type Target = LTObj;
    fn deref(&self) -> &LTObj {
        &self.data
    }
}

impl Deref for LTObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<LT> for Expr {
    fn from(x: LT) -> Expr {
        let arc = <LT as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl LT {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Layout
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Layout"]
pub struct LayoutObj {
    base: Object,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Layout {
    data: ObjectArc<LayoutObj>,
}

impl Deref for Layout {
    type Target = LayoutObj;
    fn deref(&self) -> &LayoutObj {
        &self.data
    }
}

impl Layout {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.ComposeLayout
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.ComposeLayout"]
pub struct ComposeLayoutObj {
    base: LayoutObj,
    pub swizzle: SwizzleLayout,
    pub tile_layout: TileLayout,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ComposeLayout {
    data: ObjectArc<ComposeLayoutObj>,
}

impl Deref for ComposeLayout {
    type Target = ComposeLayoutObj;
    fn deref(&self) -> &ComposeLayoutObj {
        &self.data
    }
}

impl Deref for ComposeLayoutObj {
    type Target = LayoutObj;
    fn deref(&self) -> &LayoutObj {
        &self.base
    }
}

impl From<ComposeLayout> for Layout {
    fn from(x: ComposeLayout) -> Layout {
        let arc = <ComposeLayout as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const LayoutObj)
        };
        <Layout as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl ComposeLayout {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Let
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Let"]
pub struct LetObj {
    base: ExprObj,
    pub var: Var,
    pub value: Expr,
    pub body: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Let {
    data: ObjectArc<LetObj>,
}

impl Deref for Let {
    type Target = LetObj;
    fn deref(&self) -> &LetObj {
        &self.data
    }
}

impl Deref for LetObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Let> for Expr {
    fn from(x: Let) -> Expr {
        let arc = <Let as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Let {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.MatchBufferRegion
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.MatchBufferRegion"]
pub struct MatchBufferRegionObj {
    base: Object,
    pub buffer: Buffer,
    pub source: BufferRegion,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct MatchBufferRegion {
    data: ObjectArc<MatchBufferRegionObj>,
}

impl Deref for MatchBufferRegion {
    type Target = MatchBufferRegionObj;
    fn deref(&self) -> &MatchBufferRegionObj {
        &self.data
    }
}

impl MatchBufferRegion {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> MatchBufferRegionBuilder {
        MatchBufferRegionBuilder {
            base: Object::new(),
            buffer: None,
            source: None,
        }
    }
}

pub struct MatchBufferRegionBuilder {
    base: Object,
    buffer: Option<Buffer>,
    source: Option<BufferRegion>,
}

impl MatchBufferRegionBuilder {
    pub fn buffer(mut self, buffer: Buffer) -> Self {
        self.buffer = Some(buffer);
        self
    }

    pub fn source(mut self, source: BufferRegion) -> Self {
        self.source = Some(source);
        self
    }

    pub fn build(self) -> Result<MatchBufferRegion> {
        Ok(MatchBufferRegion {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<MatchBufferRegionObj> {
        let buffer = self.buffer.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `buffer` is not set", ""))?;
        let source = self.source.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `source` is not set", ""))?;
        Ok(MatchBufferRegionObj {
            base: self.base,
            buffer,
            source,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Max
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Max"]
pub struct MaxObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Max {
    data: ObjectArc<MaxObj>,
}

impl Deref for Max {
    type Target = MaxObj;
    fn deref(&self) -> &MaxObj {
        &self.data
    }
}

impl Deref for MaxObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Max> for Expr {
    fn from(x: Max) -> Expr {
        let arc = <Max as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Max {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Min
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Min"]
pub struct MinObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Min {
    data: ObjectArc<MinObj>,
}

impl Deref for Min {
    type Target = MinObj;
    fn deref(&self) -> &MinObj {
        &self.data
    }
}

impl Deref for MinObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Min> for Expr {
    fn from(x: Min) -> Expr {
        let arc = <Min as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Min {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Mod
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Mod"]
pub struct ModObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Mod {
    data: ObjectArc<ModObj>,
}

impl Deref for Mod {
    type Target = ModObj;
    fn deref(&self) -> &ModObj {
        &self.data
    }
}

impl Deref for ModObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Mod> for Expr {
    fn from(x: Mod) -> Expr {
        let arc = <Mod as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Mod {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Mul
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Mul"]
pub struct MulObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Mul {
    data: ObjectArc<MulObj>,
}

impl Deref for Mul {
    type Target = MulObj;
    fn deref(&self) -> &MulObj {
        &self.data
    }
}

impl Deref for MulObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Mul> for Expr {
    fn from(x: Mul) -> Expr {
        let arc = <Mul as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Mul {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.NE
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.NE"]
pub struct NEObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct NE {
    data: ObjectArc<NEObj>,
}

impl Deref for NE {
    type Target = NEObj;
    fn deref(&self) -> &NEObj {
        &self.data
    }
}

impl Deref for NEObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<NE> for Expr {
    fn from(x: NE) -> Expr {
        let arc = <NE as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl NE {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Not
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Not"]
pub struct NotObj {
    base: ExprObj,
    pub a: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Not {
    data: ObjectArc<NotObj>,
}

impl Deref for Not {
    type Target = NotObj;
    fn deref(&self) -> &NotObj {
        &self.data
    }
}

impl Deref for NotObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Not> for Expr {
    fn from(x: Not) -> Expr {
        let arc = <Not as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Not {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Or
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Or"]
pub struct OrObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Or {
    data: ObjectArc<OrObj>,
}

impl Deref for Or {
    type Target = OrObj;
    fn deref(&self) -> &OrObj {
        &self.data
    }
}

impl Deref for OrObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Or> for Expr {
    fn from(x: Or) -> Expr {
        let arc = <Or as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Or {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Pipeline
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Pipeline"]
pub struct PipelineObj {
    base: Object,
    pub thread_scope: ExecScope,
    pub depth: i64,
    pub separate_pc: bool,
    pub name_hint: String,
    pub workspace: Map<String, Buffer>,
    pub schedule_config: Map<String, ObjectRef>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Pipeline {
    data: ObjectArc<PipelineObj>,
}

impl Deref for Pipeline {
    type Target = PipelineObj;
    fn deref(&self) -> &PipelineObj {
        &self.data
    }
}

impl Pipeline {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> PipelineBuilder {
        PipelineBuilder {
            base: Object::new(),
            thread_scope: None,
            depth: None,
            separate_pc: None,
            name_hint: None,
            workspace: None,
            schedule_config: None,
        }
    }
}

pub struct PipelineBuilder {
    base: Object,
    thread_scope: Option<ExecScope>,
    depth: Option<i64>,
    separate_pc: Option<bool>,
    name_hint: Option<String>,
    workspace: Option<Map<String, Buffer>>,
    schedule_config: Option<Map<String, ObjectRef>>,
}

impl PipelineBuilder {
    pub fn thread_scope(mut self, thread_scope: ExecScope) -> Self {
        self.thread_scope = Some(thread_scope);
        self
    }

    pub fn depth(mut self, depth: i64) -> Self {
        self.depth = Some(depth);
        self
    }

    pub fn separate_pc(mut self, separate_pc: bool) -> Self {
        self.separate_pc = Some(separate_pc);
        self
    }

    pub fn name_hint(mut self, name_hint: String) -> Self {
        self.name_hint = Some(name_hint);
        self
    }

    pub fn workspace(mut self, workspace: Map<String, Buffer>) -> Self {
        self.workspace = Some(workspace);
        self
    }

    pub fn schedule_config(mut self, schedule_config: Map<String, ObjectRef>) -> Self {
        self.schedule_config = Some(schedule_config);
        self
    }

    pub fn build(self) -> Result<Pipeline> {
        Ok(Pipeline {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<PipelineObj> {
        let thread_scope = self.thread_scope.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `thread_scope` is not set", ""))?;
        let depth = self.depth.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `depth` is not set", ""))?;
        let separate_pc = self.separate_pc.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `separate_pc` is not set", ""))?;
        let name_hint = self.name_hint.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `name_hint` is not set", ""))?;
        let workspace = self.workspace.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `workspace` is not set", ""))?;
        let schedule_config = self.schedule_config.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `schedule_config` is not set", ""))?;
        Ok(PipelineObj {
            base: self.base,
            thread_scope,
            depth,
            separate_pc,
            name_hint,
            workspace,
            schedule_config,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.CopyPipeline
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.CopyPipeline"]
pub struct CopyPipelineObj {
    base: PipelineObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct CopyPipeline {
    data: ObjectArc<CopyPipelineObj>,
}

impl Deref for CopyPipeline {
    type Target = CopyPipelineObj;
    fn deref(&self) -> &CopyPipelineObj {
        &self.data
    }
}

impl Deref for CopyPipelineObj {
    type Target = PipelineObj;
    fn deref(&self) -> &PipelineObj {
        &self.base
    }
}

impl From<CopyPipeline> for Pipeline {
    fn from(x: CopyPipeline) -> Pipeline {
        let arc = <CopyPipeline as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PipelineObj)
        };
        <Pipeline as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl CopyPipeline {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> CopyPipelineBuilder {
        CopyPipelineBuilder {
            base: None,
        }
    }
}

pub struct CopyPipelineBuilder {
    base: Option<PipelineObj>,
}

impl CopyPipelineBuilder {
    pub fn base(mut self, base: PipelineObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn build(self) -> Result<CopyPipeline> {
        Ok(CopyPipeline {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<CopyPipelineObj> {
        let base = match self.base {
            Some(base) => base,
            None => Pipeline::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Pipeline` construction failed: {}", e.message()),
                "",
            ))?,
        };
        Ok(CopyPipelineObj {
            base,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Predicate
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Predicate"]
pub struct PredicateObj {
    base: Object,
    pub vars: Array<Var>,
    pub pred: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Predicate {
    data: ObjectArc<PredicateObj>,
}

impl Deref for Predicate {
    type Target = PredicateObj;
    fn deref(&self) -> &PredicateObj {
        &self.data
    }
}

impl Predicate {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> PredicateBuilder {
        PredicateBuilder {
            base: Object::new(),
            vars: None,
            pred: None,
        }
    }
}

pub struct PredicateBuilder {
    base: Object,
    vars: Option<Array<Var>>,
    pred: Option<Expr>,
}

impl PredicateBuilder {
    pub fn vars(mut self, vars: Array<Var>) -> Self {
        self.vars = Some(vars);
        self
    }

    pub fn pred(mut self, pred: Expr) -> Self {
        self.pred = Some(pred);
        self
    }

    pub fn build(self) -> Result<Predicate> {
        Ok(Predicate {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<PredicateObj> {
        let vars = self.vars.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `vars` is not set", ""))?;
        let pred = self.pred.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `pred` is not set", ""))?;
        Ok(PredicateObj {
            base: self.base,
            vars,
            pred,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.PrimFunc
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.PrimFunc"]
pub struct PrimFuncObj {
    base: BaseFuncObj,
    pub params: Array<Var>,
    pub ret_type: Type,
    pub buffer_map: Map<Var, Buffer>,
    pub body: Stmt,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PrimFunc {
    data: ObjectArc<PrimFuncObj>,
}

impl Deref for PrimFunc {
    type Target = PrimFuncObj;
    fn deref(&self) -> &PrimFuncObj {
        &self.data
    }
}

impl Deref for PrimFuncObj {
    type Target = BaseFuncObj;
    fn deref(&self) -> &BaseFuncObj {
        &self.base
    }
}

impl From<PrimFunc> for BaseFunc {
    fn from(x: PrimFunc) -> BaseFunc {
        let arc = <PrimFunc as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const BaseFuncObj)
        };
        <BaseFunc as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl PrimFunc {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.PrimFuncPass
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.PrimFuncPass"]
pub struct PrimFuncPassObj {
    base: PassObj,
    pub pass_info: PassInfo,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PrimFuncPass {
    data: ObjectArc<PrimFuncPassObj>,
}

impl Deref for PrimFuncPass {
    type Target = PrimFuncPassObj;
    fn deref(&self) -> &PrimFuncPassObj {
        &self.data
    }
}

impl Deref for PrimFuncPassObj {
    type Target = PassObj;
    fn deref(&self) -> &PassObj {
        &self.base
    }
}

impl From<PrimFuncPass> for Pass {
    fn from(x: PrimFuncPass) -> Pass {
        let arc = <PrimFuncPass as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PassObj)
        };
        <Pass as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl PrimFuncPass {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.ProducerLoad
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.ProducerLoad"]
pub struct ProducerLoadObj {
    base: ExprObj,
    pub producer: DataProducer,
    pub indices: Array<Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ProducerLoad {
    data: ObjectArc<ProducerLoadObj>,
}

impl Deref for ProducerLoad {
    type Target = ProducerLoadObj;
    fn deref(&self) -> &ProducerLoadObj {
        &self.data
    }
}

impl Deref for ProducerLoadObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<ProducerLoad> for Expr {
    fn from(x: ProducerLoad) -> Expr {
        let arc = <ProducerLoad as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl ProducerLoad {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.PyStmtExprMutator
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.PyStmtExprMutator"]
pub struct PyStmtExprMutatorObj {
    base: Object,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PyStmtExprMutator {
    data: ObjectArc<PyStmtExprMutatorObj>,
}

impl Deref for PyStmtExprMutator {
    type Target = PyStmtExprMutatorObj;
    fn deref(&self) -> &PyStmtExprMutatorObj {
        &self.data
    }
}

impl DerefMut for PyStmtExprMutator {
    fn deref_mut(&mut self) -> &mut PyStmtExprMutatorObj {
        &mut self.data
    }
}

impl PyStmtExprMutator {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> PyStmtExprMutatorBuilder {
        PyStmtExprMutatorBuilder {
            base: Object::new(),
        }
    }
}

pub struct PyStmtExprMutatorBuilder {
    base: Object,
}

impl PyStmtExprMutatorBuilder {
    pub fn build(self) -> Result<PyStmtExprMutator> {
        Ok(PyStmtExprMutator {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<PyStmtExprMutatorObj> {
        Ok(PyStmtExprMutatorObj {
            base: self.base,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.PyStmtExprVisitor
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.PyStmtExprVisitor"]
pub struct PyStmtExprVisitorObj {
    base: Object,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PyStmtExprVisitor {
    data: ObjectArc<PyStmtExprVisitorObj>,
}

impl Deref for PyStmtExprVisitor {
    type Target = PyStmtExprVisitorObj;
    fn deref(&self) -> &PyStmtExprVisitorObj {
        &self.data
    }
}

impl DerefMut for PyStmtExprVisitor {
    fn deref_mut(&mut self) -> &mut PyStmtExprVisitorObj {
        &mut self.data
    }
}

impl PyStmtExprVisitor {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> PyStmtExprVisitorBuilder {
        PyStmtExprVisitorBuilder {
            base: Object::new(),
        }
    }
}

pub struct PyStmtExprVisitorBuilder {
    base: Object,
}

impl PyStmtExprVisitorBuilder {
    pub fn build(self) -> Result<PyStmtExprVisitor> {
        Ok(PyStmtExprVisitor {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<PyStmtExprVisitorObj> {
        Ok(PyStmtExprVisitorObj {
            base: self.base,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Ramp
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Ramp"]
pub struct RampObj {
    __base: ExprObj,
    pub base: Expr,
    pub stride: Expr,
    pub lanes: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Ramp {
    data: ObjectArc<RampObj>,
}

impl Deref for Ramp {
    type Target = RampObj;
    fn deref(&self) -> &RampObj {
        &self.data
    }
}

impl Deref for RampObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.__base
    }
}

impl From<Ramp> for Expr {
    fn from(x: Ramp) -> Expr {
        let arc = <Ramp as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Ramp {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Reduce
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Reduce"]
pub struct ReduceObj {
    base: ExprObj,
    pub combiner: CommReducer,
    pub source: Array<Expr>,
    pub init: Array<Expr>,
    pub axis: Array<IterVar>,
    pub condition: Expr,
    pub value_index: i32,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Reduce {
    data: ObjectArc<ReduceObj>,
}

impl Deref for Reduce {
    type Target = ReduceObj;
    fn deref(&self) -> &ReduceObj {
        &self.data
    }
}

impl Deref for ReduceObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Reduce> for Expr {
    fn from(x: Reduce) -> Expr {
        let arc = <Reduce as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Reduce {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.ScopeIdDef
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.ScopeIdDef"]
pub struct ScopeIdDefObj {
    base: Object,
    pub def_ids: Array<Var>,
    pub extents: Optional<Array<Expr>>,
    pub scope: i32,
    pub preferred_extents: Optional<Array<Expr>>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ScopeIdDef {
    data: ObjectArc<ScopeIdDefObj>,
}

impl Deref for ScopeIdDef {
    type Target = ScopeIdDefObj;
    fn deref(&self) -> &ScopeIdDefObj {
        &self.data
    }
}

impl ScopeIdDef {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> ScopeIdDefBuilder {
        ScopeIdDefBuilder {
            base: Object::new(),
            def_ids: None,
            extents: None,
            scope: None,
            preferred_extents: None,
        }
    }
}

pub struct ScopeIdDefBuilder {
    base: Object,
    def_ids: Option<Array<Var>>,
    extents: Option<Optional<Array<Expr>>>,
    scope: Option<i32>,
    preferred_extents: Option<Optional<Array<Expr>>>,
}

impl ScopeIdDefBuilder {
    pub fn def_ids(mut self, def_ids: Array<Var>) -> Self {
        self.def_ids = Some(def_ids);
        self
    }

    pub fn extents(mut self, extents: Optional<Array<Expr>>) -> Self {
        self.extents = Some(extents);
        self
    }

    pub fn scope(mut self, scope: i32) -> Self {
        self.scope = Some(scope);
        self
    }

    pub fn preferred_extents(mut self, preferred_extents: Optional<Array<Expr>>) -> Self {
        self.preferred_extents = Some(preferred_extents);
        self
    }

    pub fn build(self) -> Result<ScopeIdDef> {
        Ok(ScopeIdDef {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<ScopeIdDefObj> {
        let def_ids = self.def_ids.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `def_ids` is not set", ""))?;
        let extents = self.extents.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `extents` is not set", ""))?;
        let scope = self.scope.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `scope` is not set", ""))?;
        let preferred_extents = self.preferred_extents.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `preferred_extents` is not set", ""))?;
        Ok(ScopeIdDefObj {
            base: self.base,
            def_ids,
            extents,
            scope,
            preferred_extents,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Select
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Select"]
pub struct SelectObj {
    base: ExprObj,
    pub condition: Expr,
    pub true_value: Expr,
    pub false_value: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Select {
    data: ObjectArc<SelectObj>,
}

impl Deref for Select {
    type Target = SelectObj;
    fn deref(&self) -> &SelectObj {
        &self.data
    }
}

impl Deref for SelectObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Select> for Expr {
    fn from(x: Select) -> Expr {
        let arc = <Select as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Select {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Shuffle
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Shuffle"]
pub struct ShuffleObj {
    base: ExprObj,
    pub vectors: Array<Expr>,
    pub indices: Array<Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Shuffle {
    data: ObjectArc<ShuffleObj>,
}

impl Deref for Shuffle {
    type Target = ShuffleObj;
    fn deref(&self) -> &ShuffleObj {
        &self.data
    }
}

impl Deref for ShuffleObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Shuffle> for Expr {
    fn from(x: Shuffle) -> Expr {
        let arc = <Shuffle as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Shuffle {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Stmt
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Stmt"]
pub struct StmtObj {
    base: Object,
    pub span: Span,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Stmt {
    data: ObjectArc<StmtObj>,
}

impl Deref for Stmt {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.data
    }
}

impl Stmt {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> StmtBuilder {
        StmtBuilder {
            base: Object::new(),
            span: None,
        }
    }
}

pub struct StmtBuilder {
    base: Object,
    span: Option<Span>,
}

impl StmtBuilder {
    pub fn span(mut self, span: Span) -> Self {
        self.span = Some(span);
        self
    }

    pub fn build(self) -> Result<Stmt> {
        Ok(Stmt {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<StmtObj> {
        let span = self.span.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `span` is not set", ""))?;
        Ok(StmtObj {
            base: self.base,
            span,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.AllocBuffer
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.AllocBuffer"]
pub struct AllocBufferObj {
    base: StmtObj,
    pub buffer: Buffer,
    pub annotations: Map<String, ObjectRef>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct AllocBuffer {
    data: ObjectArc<AllocBufferObj>,
}

impl Deref for AllocBuffer {
    type Target = AllocBufferObj;
    fn deref(&self) -> &AllocBufferObj {
        &self.data
    }
}

impl Deref for AllocBufferObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<AllocBuffer> for Stmt {
    fn from(x: AllocBuffer) -> Stmt {
        let arc = <AllocBuffer as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl AllocBuffer {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> AllocBufferBuilder {
        AllocBufferBuilder {
            base: None,
            buffer: None,
            annotations: None,
        }
    }
}

pub struct AllocBufferBuilder {
    base: Option<StmtObj>,
    buffer: Option<Buffer>,
    annotations: Option<Map<String, ObjectRef>>,
}

impl AllocBufferBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn buffer(mut self, buffer: Buffer) -> Self {
        self.buffer = Some(buffer);
        self
    }

    pub fn annotations(mut self, annotations: Map<String, ObjectRef>) -> Self {
        self.annotations = Some(annotations);
        self
    }

    pub fn build(self) -> Result<AllocBuffer> {
        Ok(AllocBuffer {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<AllocBufferObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let buffer = self.buffer.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `buffer` is not set", ""))?;
        let annotations = self.annotations.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `annotations` is not set", ""))?;
        Ok(AllocBufferObj {
            base,
            buffer,
            annotations,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.AssertStmt
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.AssertStmt"]
pub struct AssertStmtObj {
    base: StmtObj,
    pub condition: Expr,
    pub error_kind: StringImm,
    pub message_parts: Array<StringImm>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct AssertStmt {
    data: ObjectArc<AssertStmtObj>,
}

impl Deref for AssertStmt {
    type Target = AssertStmtObj;
    fn deref(&self) -> &AssertStmtObj {
        &self.data
    }
}

impl Deref for AssertStmtObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<AssertStmt> for Stmt {
    fn from(x: AssertStmt) -> Stmt {
        let arc = <AssertStmt as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl AssertStmt {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> AssertStmtBuilder {
        AssertStmtBuilder {
            base: None,
            condition: None,
            error_kind: None,
            message_parts: None,
        }
    }
}

pub struct AssertStmtBuilder {
    base: Option<StmtObj>,
    condition: Option<Expr>,
    error_kind: Option<StringImm>,
    message_parts: Option<Array<StringImm>>,
}

impl AssertStmtBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn condition(mut self, condition: Expr) -> Self {
        self.condition = Some(condition);
        self
    }

    pub fn error_kind(mut self, error_kind: StringImm) -> Self {
        self.error_kind = Some(error_kind);
        self
    }

    pub fn message_parts(mut self, message_parts: Array<StringImm>) -> Self {
        self.message_parts = Some(message_parts);
        self
    }

    pub fn build(self) -> Result<AssertStmt> {
        Ok(AssertStmt {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<AssertStmtObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let condition = self.condition.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `condition` is not set", ""))?;
        let error_kind = self.error_kind.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `error_kind` is not set", ""))?;
        let message_parts = self.message_parts.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `message_parts` is not set", ""))?;
        Ok(AssertStmtObj {
            base,
            condition,
            error_kind,
            message_parts,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.AttrStmt
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.AttrStmt"]
pub struct AttrStmtObj {
    base: StmtObj,
    pub node: Any,
    pub attr_key: String,
    pub value: Expr,
    pub body: Stmt,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct AttrStmt {
    data: ObjectArc<AttrStmtObj>,
}

impl Deref for AttrStmt {
    type Target = AttrStmtObj;
    fn deref(&self) -> &AttrStmtObj {
        &self.data
    }
}

impl Deref for AttrStmtObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<AttrStmt> for Stmt {
    fn from(x: AttrStmt) -> Stmt {
        let arc = <AttrStmt as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl AttrStmt {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> AttrStmtBuilder {
        AttrStmtBuilder {
            base: None,
            node: None,
            attr_key: None,
            value: None,
            body: None,
        }
    }
}

pub struct AttrStmtBuilder {
    base: Option<StmtObj>,
    node: Option<Any>,
    attr_key: Option<String>,
    value: Option<Expr>,
    body: Option<Stmt>,
}

impl AttrStmtBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn node(mut self, node: Any) -> Self {
        self.node = Some(node);
        self
    }

    pub fn attr_key(mut self, attr_key: String) -> Self {
        self.attr_key = Some(attr_key);
        self
    }

    pub fn value(mut self, value: Expr) -> Self {
        self.value = Some(value);
        self
    }

    pub fn body(mut self, body: Stmt) -> Self {
        self.body = Some(body);
        self
    }

    pub fn build(self) -> Result<AttrStmt> {
        Ok(AttrStmt {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<AttrStmtObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let node = self.node.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `node` is not set", ""))?;
        let attr_key = self.attr_key.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `attr_key` is not set", ""))?;
        let value = self.value.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `value` is not set", ""))?;
        let body = self.body.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `body` is not set", ""))?;
        Ok(AttrStmtObj {
            base,
            node,
            attr_key,
            value,
            body,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Bind
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Bind"]
pub struct BindObj {
    base: StmtObj,
    pub var: Var,
    pub value: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Bind {
    data: ObjectArc<BindObj>,
}

impl Deref for Bind {
    type Target = BindObj;
    fn deref(&self) -> &BindObj {
        &self.data
    }
}

impl Deref for BindObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<Bind> for Stmt {
    fn from(x: Bind) -> Stmt {
        let arc = <Bind as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Bind {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> BindBuilder {
        BindBuilder {
            base: None,
            var: None,
            value: None,
        }
    }
}

pub struct BindBuilder {
    base: Option<StmtObj>,
    var: Option<Var>,
    value: Option<Expr>,
}

impl BindBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn var(mut self, var: Var) -> Self {
        self.var = Some(var);
        self
    }

    pub fn value(mut self, value: Expr) -> Self {
        self.value = Some(value);
        self
    }

    pub fn build(self) -> Result<Bind> {
        Ok(Bind {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<BindObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let var = self.var.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `var` is not set", ""))?;
        let value = self.value.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `value` is not set", ""))?;
        Ok(BindObj {
            base,
            var,
            value,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Break
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Break"]
pub struct BreakObj {
    base: StmtObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Break {
    data: ObjectArc<BreakObj>,
}

impl Deref for Break {
    type Target = BreakObj;
    fn deref(&self) -> &BreakObj {
        &self.data
    }
}

impl Deref for BreakObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<Break> for Stmt {
    fn from(x: Break) -> Stmt {
        let arc = <Break as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Break {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> BreakBuilder {
        BreakBuilder {
            base: None,
        }
    }
}

pub struct BreakBuilder {
    base: Option<StmtObj>,
}

impl BreakBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn build(self) -> Result<Break> {
        Ok(Break {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<BreakObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        Ok(BreakObj {
            base,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.BufferStore
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.BufferStore"]
pub struct BufferStoreObj {
    base: StmtObj,
    pub buffer: Buffer,
    pub value: Expr,
    pub indices: Array<Expr>,
    pub predicate: Optional<Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct BufferStore {
    data: ObjectArc<BufferStoreObj>,
}

impl Deref for BufferStore {
    type Target = BufferStoreObj;
    fn deref(&self) -> &BufferStoreObj {
        &self.data
    }
}

impl Deref for BufferStoreObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<BufferStore> for Stmt {
    fn from(x: BufferStore) -> Stmt {
        let arc = <BufferStore as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl BufferStore {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> BufferStoreBuilder {
        BufferStoreBuilder {
            base: None,
            buffer: None,
            value: None,
            indices: None,
            predicate: None,
        }
    }
}

pub struct BufferStoreBuilder {
    base: Option<StmtObj>,
    buffer: Option<Buffer>,
    value: Option<Expr>,
    indices: Option<Array<Expr>>,
    predicate: Option<Optional<Expr>>,
}

impl BufferStoreBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn buffer(mut self, buffer: Buffer) -> Self {
        self.buffer = Some(buffer);
        self
    }

    pub fn value(mut self, value: Expr) -> Self {
        self.value = Some(value);
        self
    }

    pub fn indices(mut self, indices: Array<Expr>) -> Self {
        self.indices = Some(indices);
        self
    }

    pub fn predicate(mut self, predicate: Optional<Expr>) -> Self {
        self.predicate = Some(predicate);
        self
    }

    pub fn build(self) -> Result<BufferStore> {
        Ok(BufferStore {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<BufferStoreObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let buffer = self.buffer.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `buffer` is not set", ""))?;
        let value = self.value.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `value` is not set", ""))?;
        let indices = self.indices.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `indices` is not set", ""))?;
        let predicate = self.predicate.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `predicate` is not set", ""))?;
        Ok(BufferStoreObj {
            base,
            buffer,
            value,
            indices,
            predicate,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Continue
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Continue"]
pub struct ContinueObj {
    base: StmtObj,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Continue {
    data: ObjectArc<ContinueObj>,
}

impl Deref for Continue {
    type Target = ContinueObj;
    fn deref(&self) -> &ContinueObj {
        &self.data
    }
}

impl Deref for ContinueObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<Continue> for Stmt {
    fn from(x: Continue) -> Stmt {
        let arc = <Continue as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Continue {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> ContinueBuilder {
        ContinueBuilder {
            base: None,
        }
    }
}

pub struct ContinueBuilder {
    base: Option<StmtObj>,
}

impl ContinueBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn build(self) -> Result<Continue> {
        Ok(Continue {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<ContinueObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        Ok(ContinueObj {
            base,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.DeclBuffer
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.DeclBuffer"]
pub struct DeclBufferObj {
    base: StmtObj,
    pub buffer: Buffer,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct DeclBuffer {
    data: ObjectArc<DeclBufferObj>,
}

impl Deref for DeclBuffer {
    type Target = DeclBufferObj;
    fn deref(&self) -> &DeclBufferObj {
        &self.data
    }
}

impl Deref for DeclBufferObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<DeclBuffer> for Stmt {
    fn from(x: DeclBuffer) -> Stmt {
        let arc = <DeclBuffer as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl DeclBuffer {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> DeclBufferBuilder {
        DeclBufferBuilder {
            base: None,
            buffer: None,
        }
    }
}

pub struct DeclBufferBuilder {
    base: Option<StmtObj>,
    buffer: Option<Buffer>,
}

impl DeclBufferBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn buffer(mut self, buffer: Buffer) -> Self {
        self.buffer = Some(buffer);
        self
    }

    pub fn build(self) -> Result<DeclBuffer> {
        Ok(DeclBuffer {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<DeclBufferObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let buffer = self.buffer.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `buffer` is not set", ""))?;
        Ok(DeclBufferObj {
            base,
            buffer,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Evaluate
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Evaluate"]
pub struct EvaluateObj {
    base: StmtObj,
    pub value: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Evaluate {
    data: ObjectArc<EvaluateObj>,
}

impl Deref for Evaluate {
    type Target = EvaluateObj;
    fn deref(&self) -> &EvaluateObj {
        &self.data
    }
}

impl Deref for EvaluateObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<Evaluate> for Stmt {
    fn from(x: Evaluate) -> Stmt {
        let arc = <Evaluate as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Evaluate {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> EvaluateBuilder {
        EvaluateBuilder {
            base: None,
            value: None,
        }
    }
}

pub struct EvaluateBuilder {
    base: Option<StmtObj>,
    value: Option<Expr>,
}

impl EvaluateBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn value(mut self, value: Expr) -> Self {
        self.value = Some(value);
        self
    }

    pub fn build(self) -> Result<Evaluate> {
        Ok(Evaluate {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<EvaluateObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let value = self.value.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `value` is not set", ""))?;
        Ok(EvaluateObj {
            base,
            value,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.For
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.For"]
pub struct ForObj {
    base: StmtObj,
    pub loop_var: Var,
    pub min: Expr,
    pub extent: Expr,
    pub kind: i32,
    pub body: Stmt,
    pub thread_binding: Optional<IterVar>,
    pub annotations: Map<String, ObjectRef>,
    pub step: Optional<Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct For {
    data: ObjectArc<ForObj>,
}

impl Deref for For {
    type Target = ForObj;
    fn deref(&self) -> &ForObj {
        &self.data
    }
}

impl Deref for ForObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<For> for Stmt {
    fn from(x: For) -> Stmt {
        let arc = <For as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl For {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> ForBuilder {
        ForBuilder {
            base: None,
            loop_var: None,
            min: None,
            extent: None,
            kind: None,
            body: None,
            thread_binding: None,
            annotations: None,
            step: None,
        }
    }
}

pub struct ForBuilder {
    base: Option<StmtObj>,
    loop_var: Option<Var>,
    min: Option<Expr>,
    extent: Option<Expr>,
    kind: Option<i32>,
    body: Option<Stmt>,
    thread_binding: Option<Optional<IterVar>>,
    annotations: Option<Map<String, ObjectRef>>,
    step: Option<Optional<Expr>>,
}

impl ForBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn loop_var(mut self, loop_var: Var) -> Self {
        self.loop_var = Some(loop_var);
        self
    }

    pub fn min(mut self, min: Expr) -> Self {
        self.min = Some(min);
        self
    }

    pub fn extent(mut self, extent: Expr) -> Self {
        self.extent = Some(extent);
        self
    }

    pub fn kind(mut self, kind: i32) -> Self {
        self.kind = Some(kind);
        self
    }

    pub fn body(mut self, body: Stmt) -> Self {
        self.body = Some(body);
        self
    }

    pub fn thread_binding(mut self, thread_binding: Optional<IterVar>) -> Self {
        self.thread_binding = Some(thread_binding);
        self
    }

    pub fn annotations(mut self, annotations: Map<String, ObjectRef>) -> Self {
        self.annotations = Some(annotations);
        self
    }

    pub fn step(mut self, step: Optional<Expr>) -> Self {
        self.step = Some(step);
        self
    }

    pub fn build(self) -> Result<For> {
        Ok(For {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<ForObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let loop_var = self.loop_var.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `loop_var` is not set", ""))?;
        let min = self.min.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `min` is not set", ""))?;
        let extent = self.extent.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `extent` is not set", ""))?;
        let kind = self.kind.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `kind` is not set", ""))?;
        let body = self.body.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `body` is not set", ""))?;
        let thread_binding = self.thread_binding.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `thread_binding` is not set", ""))?;
        let annotations = self.annotations.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `annotations` is not set", ""))?;
        let step = self.step.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `step` is not set", ""))?;
        Ok(ForObj {
            base,
            loop_var,
            min,
            extent,
            kind,
            body,
            thread_binding,
            annotations,
            step,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.IfThenElse
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.IfThenElse"]
pub struct IfThenElseObj {
    base: StmtObj,
    pub condition: Expr,
    pub then_case: Stmt,
    pub else_case: Optional<Stmt>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct IfThenElse {
    data: ObjectArc<IfThenElseObj>,
}

impl Deref for IfThenElse {
    type Target = IfThenElseObj;
    fn deref(&self) -> &IfThenElseObj {
        &self.data
    }
}

impl Deref for IfThenElseObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<IfThenElse> for Stmt {
    fn from(x: IfThenElse) -> Stmt {
        let arc = <IfThenElse as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl IfThenElse {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> IfThenElseBuilder {
        IfThenElseBuilder {
            base: None,
            condition: None,
            then_case: None,
            else_case: None,
        }
    }
}

pub struct IfThenElseBuilder {
    base: Option<StmtObj>,
    condition: Option<Expr>,
    then_case: Option<Stmt>,
    else_case: Option<Optional<Stmt>>,
}

impl IfThenElseBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn condition(mut self, condition: Expr) -> Self {
        self.condition = Some(condition);
        self
    }

    pub fn then_case(mut self, then_case: Stmt) -> Self {
        self.then_case = Some(then_case);
        self
    }

    pub fn else_case(mut self, else_case: Optional<Stmt>) -> Self {
        self.else_case = Some(else_case);
        self
    }

    pub fn build(self) -> Result<IfThenElse> {
        Ok(IfThenElse {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<IfThenElseObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let condition = self.condition.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `condition` is not set", ""))?;
        let then_case = self.then_case.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `then_case` is not set", ""))?;
        let else_case = self.else_case.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `else_case` is not set", ""))?;
        Ok(IfThenElseObj {
            base,
            condition,
            then_case,
            else_case,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.SBlock
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.SBlock"]
pub struct SBlockObj {
    base: StmtObj,
    pub iter_vars: Array<IterVar>,
    pub reads: Array<BufferRegion>,
    pub writes: Array<BufferRegion>,
    pub name_hint: String,
    pub alloc_buffers: Array<Buffer>,
    pub match_buffers: Array<MatchBufferRegion>,
    pub annotations: Map<String, ObjectRef>,
    pub init: Optional<Stmt>,
    pub body: Stmt,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct SBlock {
    data: ObjectArc<SBlockObj>,
}

impl Deref for SBlock {
    type Target = SBlockObj;
    fn deref(&self) -> &SBlockObj {
        &self.data
    }
}

impl Deref for SBlockObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<SBlock> for Stmt {
    fn from(x: SBlock) -> Stmt {
        let arc = <SBlock as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl SBlock {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> SBlockBuilder {
        SBlockBuilder {
            base: None,
            iter_vars: None,
            reads: None,
            writes: None,
            name_hint: None,
            alloc_buffers: None,
            match_buffers: None,
            annotations: None,
            init: None,
            body: None,
        }
    }
}

pub struct SBlockBuilder {
    base: Option<StmtObj>,
    iter_vars: Option<Array<IterVar>>,
    reads: Option<Array<BufferRegion>>,
    writes: Option<Array<BufferRegion>>,
    name_hint: Option<String>,
    alloc_buffers: Option<Array<Buffer>>,
    match_buffers: Option<Array<MatchBufferRegion>>,
    annotations: Option<Map<String, ObjectRef>>,
    init: Option<Optional<Stmt>>,
    body: Option<Stmt>,
}

impl SBlockBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn iter_vars(mut self, iter_vars: Array<IterVar>) -> Self {
        self.iter_vars = Some(iter_vars);
        self
    }

    pub fn reads(mut self, reads: Array<BufferRegion>) -> Self {
        self.reads = Some(reads);
        self
    }

    pub fn writes(mut self, writes: Array<BufferRegion>) -> Self {
        self.writes = Some(writes);
        self
    }

    pub fn name_hint(mut self, name_hint: String) -> Self {
        self.name_hint = Some(name_hint);
        self
    }

    pub fn alloc_buffers(mut self, alloc_buffers: Array<Buffer>) -> Self {
        self.alloc_buffers = Some(alloc_buffers);
        self
    }

    pub fn match_buffers(mut self, match_buffers: Array<MatchBufferRegion>) -> Self {
        self.match_buffers = Some(match_buffers);
        self
    }

    pub fn annotations(mut self, annotations: Map<String, ObjectRef>) -> Self {
        self.annotations = Some(annotations);
        self
    }

    pub fn init(mut self, init: Optional<Stmt>) -> Self {
        self.init = Some(init);
        self
    }

    pub fn body(mut self, body: Stmt) -> Self {
        self.body = Some(body);
        self
    }

    pub fn build(self) -> Result<SBlock> {
        Ok(SBlock {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<SBlockObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let iter_vars = self.iter_vars.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `iter_vars` is not set", ""))?;
        let reads = self.reads.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `reads` is not set", ""))?;
        let writes = self.writes.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `writes` is not set", ""))?;
        let name_hint = self.name_hint.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `name_hint` is not set", ""))?;
        let alloc_buffers = self.alloc_buffers.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `alloc_buffers` is not set", ""))?;
        let match_buffers = self.match_buffers.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `match_buffers` is not set", ""))?;
        let annotations = self.annotations.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `annotations` is not set", ""))?;
        let init = self.init.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `init` is not set", ""))?;
        let body = self.body.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `body` is not set", ""))?;
        Ok(SBlockObj {
            base,
            iter_vars,
            reads,
            writes,
            name_hint,
            alloc_buffers,
            match_buffers,
            annotations,
            init,
            body,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.SBlockRealize
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.SBlockRealize"]
pub struct SBlockRealizeObj {
    base: StmtObj,
    pub iter_values: Array<Expr>,
    pub predicate: Expr,
    pub block: SBlock,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct SBlockRealize {
    data: ObjectArc<SBlockRealizeObj>,
}

impl Deref for SBlockRealize {
    type Target = SBlockRealizeObj;
    fn deref(&self) -> &SBlockRealizeObj {
        &self.data
    }
}

impl Deref for SBlockRealizeObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<SBlockRealize> for Stmt {
    fn from(x: SBlockRealize) -> Stmt {
        let arc = <SBlockRealize as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl SBlockRealize {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> SBlockRealizeBuilder {
        SBlockRealizeBuilder {
            base: None,
            iter_values: None,
            predicate: None,
            block: None,
        }
    }
}

pub struct SBlockRealizeBuilder {
    base: Option<StmtObj>,
    iter_values: Option<Array<Expr>>,
    predicate: Option<Expr>,
    block: Option<SBlock>,
}

impl SBlockRealizeBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn iter_values(mut self, iter_values: Array<Expr>) -> Self {
        self.iter_values = Some(iter_values);
        self
    }

    pub fn predicate(mut self, predicate: Expr) -> Self {
        self.predicate = Some(predicate);
        self
    }

    pub fn block(mut self, block: SBlock) -> Self {
        self.block = Some(block);
        self
    }

    pub fn build(self) -> Result<SBlockRealize> {
        Ok(SBlockRealize {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<SBlockRealizeObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let iter_values = self.iter_values.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `iter_values` is not set", ""))?;
        let predicate = self.predicate.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `predicate` is not set", ""))?;
        let block = self.block.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `block` is not set", ""))?;
        Ok(SBlockRealizeObj {
            base,
            iter_values,
            predicate,
            block,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.ScopeIdDefStmt
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.ScopeIdDefStmt"]
pub struct ScopeIdDefStmtObj {
    base: StmtObj,
    pub def: ScopeIdDef,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ScopeIdDefStmt {
    data: ObjectArc<ScopeIdDefStmtObj>,
}

impl Deref for ScopeIdDefStmt {
    type Target = ScopeIdDefStmtObj;
    fn deref(&self) -> &ScopeIdDefStmtObj {
        &self.data
    }
}

impl Deref for ScopeIdDefStmtObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<ScopeIdDefStmt> for Stmt {
    fn from(x: ScopeIdDefStmt) -> Stmt {
        let arc = <ScopeIdDefStmt as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl ScopeIdDefStmt {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> ScopeIdDefStmtBuilder {
        ScopeIdDefStmtBuilder {
            base: None,
            def: None,
        }
    }
}

pub struct ScopeIdDefStmtBuilder {
    base: Option<StmtObj>,
    def: Option<ScopeIdDef>,
}

impl ScopeIdDefStmtBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn def(mut self, def: ScopeIdDef) -> Self {
        self.def = Some(def);
        self
    }

    pub fn build(self) -> Result<ScopeIdDefStmt> {
        Ok(ScopeIdDefStmt {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<ScopeIdDefStmtObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let def = self.def.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `def` is not set", ""))?;
        Ok(ScopeIdDefStmtObj {
            base,
            def,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.SeqStmt
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.SeqStmt"]
pub struct SeqStmtObj {
    base: StmtObj,
    pub seq: Array<Stmt>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct SeqStmt {
    data: ObjectArc<SeqStmtObj>,
}

impl Deref for SeqStmt {
    type Target = SeqStmtObj;
    fn deref(&self) -> &SeqStmtObj {
        &self.data
    }
}

impl Deref for SeqStmtObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<SeqStmt> for Stmt {
    fn from(x: SeqStmt) -> Stmt {
        let arc = <SeqStmt as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl SeqStmt {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> SeqStmtBuilder {
        SeqStmtBuilder {
            base: None,
            seq: None,
        }
    }
}

pub struct SeqStmtBuilder {
    base: Option<StmtObj>,
    seq: Option<Array<Stmt>>,
}

impl SeqStmtBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn seq(mut self, seq: Array<Stmt>) -> Self {
        self.seq = Some(seq);
        self
    }

    pub fn build(self) -> Result<SeqStmt> {
        Ok(SeqStmt {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<SeqStmtObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let seq = self.seq.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `seq` is not set", ""))?;
        Ok(SeqStmtObj {
            base,
            seq,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.StringImm
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.StringImm"]
pub struct StringImmObj {
    base: ExprObj,
    pub value: String,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct StringImm {
    data: ObjectArc<StringImmObj>,
}

impl Deref for StringImm {
    type Target = StringImmObj;
    fn deref(&self) -> &StringImmObj {
        &self.data
    }
}

impl Deref for StringImmObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<StringImm> for Expr {
    fn from(x: StringImm) -> Expr {
        let arc = <StringImm as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl StringImm {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Sub
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Sub"]
pub struct SubObj {
    base: ExprObj,
    pub a: Expr,
    pub b: Expr,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Sub {
    data: ObjectArc<SubObj>,
}

impl Deref for Sub {
    type Target = SubObj;
    fn deref(&self) -> &SubObj {
        &self.data
    }
}

impl Deref for SubObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Sub> for Expr {
    fn from(x: Sub) -> Expr {
        let arc = <Sub as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Sub {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.SwizzleLayout
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.SwizzleLayout"]
pub struct SwizzleLayoutObj {
    base: LayoutObj,
    pub per_element: i32,
    pub swizzle_len: i32,
    pub atom_len: i32,
    pub swizzle_inner: bool,
    pub inner_mask: i32,
    pub outer_mask: i32,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct SwizzleLayout {
    data: ObjectArc<SwizzleLayoutObj>,
}

impl Deref for SwizzleLayout {
    type Target = SwizzleLayoutObj;
    fn deref(&self) -> &SwizzleLayoutObj {
        &self.data
    }
}

impl Deref for SwizzleLayoutObj {
    type Target = LayoutObj;
    fn deref(&self) -> &LayoutObj {
        &self.base
    }
}

impl From<SwizzleLayout> for Layout {
    fn from(x: SwizzleLayout) -> Layout {
        let arc = <SwizzleLayout as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const LayoutObj)
        };
        <Layout as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl SwizzleLayout {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.TensorIntrin
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.TensorIntrin"]
pub struct TensorIntrinObj {
    base: Object,
    pub desc: PrimFunc,
    pub r#impl: PrimFunc,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct TensorIntrin {
    data: ObjectArc<TensorIntrinObj>,
}

impl Deref for TensorIntrin {
    type Target = TensorIntrinObj;
    fn deref(&self) -> &TensorIntrinObj {
        &self.data
    }
}

impl TensorIntrin {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> TensorIntrinBuilder {
        TensorIntrinBuilder {
            base: Object::new(),
            desc: None,
            r#impl: None,
        }
    }
}

pub struct TensorIntrinBuilder {
    base: Object,
    desc: Option<PrimFunc>,
    r#impl: Option<PrimFunc>,
}

impl TensorIntrinBuilder {
    pub fn desc(mut self, desc: PrimFunc) -> Self {
        self.desc = Some(desc);
        self
    }

    pub fn r#impl(mut self, r#impl: PrimFunc) -> Self {
        self.r#impl = Some(r#impl);
        self
    }

    pub fn build(self) -> Result<TensorIntrin> {
        Ok(TensorIntrin {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<TensorIntrinObj> {
        let desc = self.desc.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `desc` is not set", ""))?;
        let r#impl = self.r#impl.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `impl` is not set", ""))?;
        Ok(TensorIntrinObj {
            base: self.base,
            desc,
            r#impl,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.TileLayout
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.TileLayout"]
pub struct TileLayoutObj {
    base: LayoutObj,
    pub shard: Array<Iter>,
    pub replica: Array<Iter>,
    pub offset: Map<Axis, Expr>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct TileLayout {
    data: ObjectArc<TileLayoutObj>,
}

impl Deref for TileLayout {
    type Target = TileLayoutObj;
    fn deref(&self) -> &TileLayoutObj {
        &self.data
    }
}

impl Deref for TileLayoutObj {
    type Target = LayoutObj;
    fn deref(&self) -> &LayoutObj {
        &self.base
    }
}

impl From<TileLayout> for Layout {
    fn from(x: TileLayout) -> Layout {
        let arc = <TileLayout as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const LayoutObj)
        };
        <Layout as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl TileLayout {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.TilePrimitiveCall
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.TilePrimitiveCall"]
pub struct TilePrimitiveCallObj {
    base: StmtObj,
    pub op: Op,
    pub args: Array<ObjectRef>,
    pub workspace: Map<String, Buffer>,
    pub config: Map<String, ObjectRef>,
    pub dispatch: Optional<String>,
    pub scope: ExecScope,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct TilePrimitiveCall {
    data: ObjectArc<TilePrimitiveCallObj>,
}

impl Deref for TilePrimitiveCall {
    type Target = TilePrimitiveCallObj;
    fn deref(&self) -> &TilePrimitiveCallObj {
        &self.data
    }
}

impl Deref for TilePrimitiveCallObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<TilePrimitiveCall> for Stmt {
    fn from(x: TilePrimitiveCall) -> Stmt {
        let arc = <TilePrimitiveCall as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl TilePrimitiveCall {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.Var
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.Var"]
pub struct VarObj {
    base: ExprObj,
    pub name: String,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Var {
    data: ObjectArc<VarObj>,
}

impl Deref for Var {
    type Target = VarObj;
    fn deref(&self) -> &VarObj {
        &self.data
    }
}

impl Deref for VarObj {
    type Target = ExprObj;
    fn deref(&self) -> &ExprObj {
        &self.base
    }
}

impl From<Var> for Expr {
    fn from(x: Var) -> Expr {
        let arc = <Var as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const ExprObj)
        };
        <Expr as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Var {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/tirx.While
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "tirx.While"]
pub struct WhileObj {
    base: StmtObj,
    pub condition: Expr,
    pub body: Stmt,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct While {
    data: ObjectArc<WhileObj>,
}

impl Deref for While {
    type Target = WhileObj;
    fn deref(&self) -> &WhileObj {
        &self.data
    }
}

impl Deref for WhileObj {
    type Target = StmtObj;
    fn deref(&self) -> &StmtObj {
        &self.base
    }
}

impl From<While> for Stmt {
    fn from(x: While) -> Stmt {
        let arc = <While as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const StmtObj)
        };
        <Stmt as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl While {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> WhileBuilder {
        WhileBuilder {
            base: None,
            condition: None,
            body: None,
        }
    }
}

pub struct WhileBuilder {
    base: Option<StmtObj>,
    condition: Option<Expr>,
    body: Option<Stmt>,
}

impl WhileBuilder {
    pub fn base(mut self, base: StmtObj) -> Self {
        self.base = Some(base);
        self
    }

    pub fn condition(mut self, condition: Expr) -> Self {
        self.condition = Some(condition);
        self
    }

    pub fn body(mut self, body: Stmt) -> Self {
        self.body = Some(body);
        self
    }

    pub fn build(self) -> Result<While> {
        Ok(While {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<WhileObj> {
        let base = match self.base {
            Some(base) => base,
            None => Stmt::ffi_new().build_obj().map_err(|e| tvm_ffi::Error::new(
                tvm_ffi::VALUE_ERROR,
                &format!("field `base` is not set and default `Stmt` construction failed: {}", e.message()),
                "",
            ))?,
        };
        let condition = self.condition.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `condition` is not set", ""))?;
        let body = self.body.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `body` is not set", ""))?;
        Ok(WhileObj {
            base,
            condition,
            body,
        })
    }
}
// tvm-ffi-stubgen(end)


pub mod analysis;
pub mod backend;
pub mod intrinsics;
pub mod transform;
