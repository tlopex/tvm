#![allow(dead_code, unused_imports)]
/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */

//! FFI bindings for `transform` (generated by tvm-ffi-stubgen).

// tvm-ffi-stubgen(begin): import-section
use std::ops::Deref;
use super::instrument::PassInstrument;
use tvm_ffi::Array;
use tvm_ffi::Map;
use tvm_ffi::Object;
use tvm_ffi::ObjectArc;
use tvm_ffi::ObjectCore;
use tvm_ffi::ObjectRefCore;
use tvm_ffi::Result;
use tvm_ffi::String;
use tvm_ffi::object::ObjectRef;
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.Pass
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.Pass"]
pub struct PassObj {
    base: Object,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Pass {
    data: ObjectArc<PassObj>,
}

impl Deref for Pass {
    type Target = PassObj;
    fn deref(&self) -> &PassObj {
        &self.data
    }
}

impl Pass {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.ModulePass
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.ModulePass"]
pub struct ModulePassObj {
    base: PassObj,
    pub pass_info: PassInfo,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ModulePass {
    data: ObjectArc<ModulePassObj>,
}

impl Deref for ModulePass {
    type Target = ModulePassObj;
    fn deref(&self) -> &ModulePassObj {
        &self.data
    }
}

impl Deref for ModulePassObj {
    type Target = PassObj;
    fn deref(&self) -> &PassObj {
        &self.base
    }
}

impl From<ModulePass> for Pass {
    fn from(x: ModulePass) -> Pass {
        let arc = <ModulePass as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PassObj)
        };
        <Pass as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl ModulePass {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.PassContext
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.PassContext"]
pub struct PassContextObj {
    base: Object,
    pub opt_level: i32,
    pub required_pass: Array<String>,
    pub disabled_pass: Array<String>,
    pub config: Map<String, ObjectRef>,
    pub instruments: Array<PassInstrument>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PassContext {
    data: ObjectArc<PassContextObj>,
}

impl Deref for PassContext {
    type Target = PassContextObj;
    fn deref(&self) -> &PassContextObj {
        &self.data
    }
}

impl PassContext {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn new(opt_level: i32, required_pass: Array<String>, disabled_pass: Array<String>, config: Map<String, ObjectRef>, instruments: Array<PassInstrument>) -> Self {
        Self {
            data: ObjectArc::new(PassContextObj {
                base: Object::new(),
                opt_level,
                required_pass,
                disabled_pass,
                config,
                instruments,
            }),
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.PassInfo
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.PassInfo"]
pub struct PassInfoObj {
    base: Object,
    pub opt_level: i32,
    pub name: String,
    pub traceable: bool,
    pub required: Array<String>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PassInfo {
    data: ObjectArc<PassInfoObj>,
}

impl Deref for PassInfo {
    type Target = PassInfoObj;
    fn deref(&self) -> &PassInfoObj {
        &self.data
    }
}

impl PassInfo {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn new(opt_level: i32, name: String, traceable: bool, required: Array<String>) -> Self {
        Self {
            data: ObjectArc::new(PassInfoObj {
                base: Object::new(),
                opt_level,
                name,
                traceable,
                required,
            }),
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.Sequential
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.Sequential"]
pub struct SequentialObj {
    base: PassObj,
    pub pass_info: PassInfo,
    pub passes: Array<Pass>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Sequential {
    data: ObjectArc<SequentialObj>,
}

impl Deref for Sequential {
    type Target = SequentialObj;
    fn deref(&self) -> &SequentialObj {
        &self.data
    }
}

impl Deref for SequentialObj {
    type Target = PassObj;
    fn deref(&self) -> &PassObj {
        &self.base
    }
}

impl From<Sequential> for Pass {
    fn from(x: Sequential) -> Pass {
        let arc = <Sequential as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PassObj)
        };
        <Pass as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Sequential {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)
