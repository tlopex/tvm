#![allow(dead_code, unused_imports)]
/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */


//! FFI bindings for `transform` (generated by tvm-ffi-stubgen).

// tvm-ffi-stubgen(begin): import-section
use std::ops::Deref;
use super::instrument::PassInstrument;
use tvm_ffi::Array;
use tvm_ffi::Map;
use tvm_ffi::Object;
use tvm_ffi::ObjectArc;
use tvm_ffi::ObjectCore;
use tvm_ffi::ObjectRefCore;
use tvm_ffi::Result;
use tvm_ffi::String;
use tvm_ffi::object::ObjectRef;
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.Pass
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.Pass"]
pub struct PassObj {
    base: Object,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Pass {
    data: ObjectArc<PassObj>,
}

impl Deref for Pass {
    type Target = PassObj;
    fn deref(&self) -> &PassObj {
        &self.data
    }
}

impl Pass {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.ModulePass
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.ModulePass"]
pub struct ModulePassObj {
    base: PassObj,
    pub pass_info: PassInfo,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct ModulePass {
    data: ObjectArc<ModulePassObj>,
}

impl Deref for ModulePass {
    type Target = ModulePassObj;
    fn deref(&self) -> &ModulePassObj {
        &self.data
    }
}

impl Deref for ModulePassObj {
    type Target = PassObj;
    fn deref(&self) -> &PassObj {
        &self.base
    }
}

impl From<ModulePass> for Pass {
    fn from(x: ModulePass) -> Pass {
        let arc = <ModulePass as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PassObj)
        };
        <Pass as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl ModulePass {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.PassContext
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.PassContext"]
pub struct PassContextObj {
    base: Object,
    pub opt_level: i32,
    pub required_pass: Array<String>,
    pub disabled_pass: Array<String>,
    pub config: Map<String, ObjectRef>,
    pub instruments: Array<PassInstrument>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PassContext {
    data: ObjectArc<PassContextObj>,
}

impl Deref for PassContext {
    type Target = PassContextObj;
    fn deref(&self) -> &PassContextObj {
        &self.data
    }
}

impl PassContext {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> PassContextBuilder {
        PassContextBuilder {
            base: Object::new(),
            opt_level: None,
            required_pass: None,
            disabled_pass: None,
            config: None,
            instruments: None,
        }
    }
}

pub struct PassContextBuilder {
    base: Object,
    opt_level: Option<i32>,
    required_pass: Option<Array<String>>,
    disabled_pass: Option<Array<String>>,
    config: Option<Map<String, ObjectRef>>,
    instruments: Option<Array<PassInstrument>>,
}

impl PassContextBuilder {
    pub fn opt_level(mut self, opt_level: i32) -> Self {
        self.opt_level = Some(opt_level);
        self
    }

    pub fn required_pass(mut self, required_pass: Array<String>) -> Self {
        self.required_pass = Some(required_pass);
        self
    }

    pub fn disabled_pass(mut self, disabled_pass: Array<String>) -> Self {
        self.disabled_pass = Some(disabled_pass);
        self
    }

    pub fn config(mut self, config: Map<String, ObjectRef>) -> Self {
        self.config = Some(config);
        self
    }

    pub fn instruments(mut self, instruments: Array<PassInstrument>) -> Self {
        self.instruments = Some(instruments);
        self
    }

    pub fn build(self) -> Result<PassContext> {
        Ok(PassContext {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<PassContextObj> {
        let opt_level = self.opt_level.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `opt_level` is not set", ""))?;
        let required_pass = self.required_pass.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `required_pass` is not set", ""))?;
        let disabled_pass = self.disabled_pass.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `disabled_pass` is not set", ""))?;
        let config = self.config.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `config` is not set", ""))?;
        let instruments = self.instruments.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `instruments` is not set", ""))?;
        Ok(PassContextObj {
            base: self.base,
            opt_level,
            required_pass,
            disabled_pass,
            config,
            instruments,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.PassInfo
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.PassInfo"]
pub struct PassInfoObj {
    base: Object,
    pub opt_level: i32,
    pub name: String,
    pub traceable: bool,
    pub required: Array<String>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct PassInfo {
    data: ObjectArc<PassInfoObj>,
}

impl Deref for PassInfo {
    type Target = PassInfoObj;
    fn deref(&self) -> &PassInfoObj {
        &self.data
    }
}

impl PassInfo {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }

    pub fn ffi_new() -> PassInfoBuilder {
        PassInfoBuilder {
            base: Object::new(),
            opt_level: None,
            name: None,
            traceable: None,
            required: None,
        }
    }
}

pub struct PassInfoBuilder {
    base: Object,
    opt_level: Option<i32>,
    name: Option<String>,
    traceable: Option<bool>,
    required: Option<Array<String>>,
}

impl PassInfoBuilder {
    pub fn opt_level(mut self, opt_level: i32) -> Self {
        self.opt_level = Some(opt_level);
        self
    }

    pub fn name(mut self, name: String) -> Self {
        self.name = Some(name);
        self
    }

    pub fn traceable(mut self, traceable: bool) -> Self {
        self.traceable = Some(traceable);
        self
    }

    pub fn required(mut self, required: Array<String>) -> Self {
        self.required = Some(required);
        self
    }

    pub fn build(self) -> Result<PassInfo> {
        Ok(PassInfo {
            data: ObjectArc::new(self.build_obj()?),
        })
    }

    pub fn build_obj(self) -> Result<PassInfoObj> {
        let opt_level = self.opt_level.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `opt_level` is not set", ""))?;
        let name = self.name.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `name` is not set", ""))?;
        let traceable = self.traceable.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `traceable` is not set", ""))?;
        let required = self.required.ok_or_else(|| tvm_ffi::Error::new(tvm_ffi::VALUE_ERROR, "field `required` is not set", ""))?;
        Ok(PassInfoObj {
            base: self.base,
            opt_level,
            name,
            traceable,
            required,
        })
    }
}
// tvm-ffi-stubgen(end)

// tvm-ffi-stubgen(begin): object/transform.Sequential
#[repr(C)]
#[derive(tvm_ffi::derive::Object)]
#[type_key = "transform.Sequential"]
pub struct SequentialObj {
    base: PassObj,
    pub pass_info: PassInfo,
    pub passes: Array<Pass>,
}

#[repr(C)]
#[derive(tvm_ffi::derive::ObjectRef, Clone)]
pub struct Sequential {
    data: ObjectArc<SequentialObj>,
}

impl Deref for Sequential {
    type Target = SequentialObj;
    fn deref(&self) -> &SequentialObj {
        &self.data
    }
}

impl Deref for SequentialObj {
    type Target = PassObj;
    fn deref(&self) -> &PassObj {
        &self.base
    }
}

impl From<Sequential> for Pass {
    fn from(x: Sequential) -> Pass {
        let arc = <Sequential as tvm_ffi::ObjectRefCore>::into_data(x);
        let up = unsafe {
            ObjectArc::from_raw(ObjectArc::into_raw(arc) as *const PassObj)
        };
        <Pass as tvm_ffi::ObjectRefCore>::from_data(up)
    }
}

impl Sequential {
    /// C++ `ObjectRef::same_as`: pointer identity of the underlying object.
    pub fn same_as<O: tvm_ffi::ObjectRefCore>(&self, other: &O) -> bool {
        unsafe {
            ObjectArc::as_raw(&self.data) as *const u8
                == ObjectArc::as_raw(<O as tvm_ffi::ObjectRefCore>::data(other)) as *const u8
        }
    }

    /// Checked downcast to a concrete object `N` (C++ `obj.as<N>()`):
    /// `Some(&N)` iff the runtime header type index matches, else `None`.
    pub fn downcast<N: tvm_ffi::ObjectCore>(&self) -> Option<&N> {
        unsafe {
            let raw = ObjectArc::as_raw(&self.data) as *const N;
            let header = raw as *const tvm_ffi::tvm_ffi_sys::TVMFFIObject;
            if (*header).type_index == <N as tvm_ffi::ObjectCore>::type_index() {
                Some(&*raw)
            } else {
                None
            }
        }
    }
}
// tvm-ffi-stubgen(end)
